// Generated by OABuilder
package com.viaoa.appstore.model;

import java.util.logging.*;
import com.viaoa.object.*;
import com.viaoa.annotation.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.appstore.model.oa.search.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.model.search.*;
import com.viaoa.appstore.model.filter.*;
import com.viaoa.appstore.delegate.ModelDelegate;
import com.viaoa.appstore.resource.Resource;

public class VersionFileModel extends OAObjectModel {
    private static Logger LOG = Logger.getLogger(VersionFileModel.class.getName());
    
    // Hubs
    protected Hub<VersionFile> hub;
    // selected versionFiles
    protected Hub<VersionFile> hubMultiSelect;
    // detail hubs
    protected Hub<ApplicationVersion> hubApplicationVersion;
    
    // ObjectModels
    protected ApplicationVersionModel modelApplicationVersion;
    
    // SearchModels used for references
    protected ApplicationVersionSearchModel modelApplicationVersionSearch;
    
    public VersionFileModel() {
        setDisplayName("Version File");
        setPluralDisplayName("Version Files");
    }
    
    public VersionFileModel(Hub<VersionFile> hubVersionFile) {
        this();
        if (hubVersionFile != null) HubDelegate.setObjectClass(hubVersionFile, VersionFile.class);
        this.hub = hubVersionFile;
    }
    public VersionFileModel(VersionFile versionFile) {
        this();
        getHub().add(versionFile);
        getHub().setPos(0);
    }
    
    public Hub<VersionFile> getOriginalHub() {
        return getHub();
    }
    
    public Hub<ApplicationVersion> getApplicationVersionHub() {
        if (hubApplicationVersion != null) return hubApplicationVersion;
        // this is the owner, use detailHub
        hubApplicationVersion = getHub().getDetailHub(VersionFile.P_ApplicationVersion);
        return hubApplicationVersion;
    }
    public VersionFile getVersionFile() {
        return getHub().getAO();
    }
    
    public Hub<VersionFile> getHub() {
        if (hub == null) {
            hub = new Hub<VersionFile>(VersionFile.class);
        }
        return hub;
    }
    
    public Hub<VersionFile> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<VersionFile>(VersionFile.class);
        }
        return hubMultiSelect;
    }
    
    public ApplicationVersionModel getApplicationVersionModel() {
        if (modelApplicationVersion != null) return modelApplicationVersion;
        modelApplicationVersion = new ApplicationVersionModel(getApplicationVersionHub());
        modelApplicationVersion.setDisplayName("Application Version");
        modelApplicationVersion.setPluralDisplayName("Application Versions");
        modelApplicationVersion.setForJfc(getForJfc());
        modelApplicationVersion.setAllowNew(false);
        modelApplicationVersion.setAllowSave(true);
        modelApplicationVersion.setAllowAdd(false);
        modelApplicationVersion.setAllowRemove(false);
        modelApplicationVersion.setAllowClear(false);
        modelApplicationVersion.setAllowDelete(false);
        modelApplicationVersion.setAllowSearch(false);
        modelApplicationVersion.setAllowHubSearch(true);
        modelApplicationVersion.setAllowGotoEdit(true);
        OALinkInfo li = HubDetailDelegate.getLinkInfoFromDetailToMaster(getOriginalHub());
        modelApplicationVersion.setCreateUI(li == null || !VersionFile.P_ApplicationVersion.equalsIgnoreCase(li.getName()) );
        modelApplicationVersion.setViewOnly(getViewOnly());
        // call VersionFile.applicationVersionModelCallback(ApplicationVersionModel) to be able to customize this model
        OAObjectCallbackDelegate.onObjectCallbackModel(VersionFile.class, VersionFile.P_ApplicationVersion, modelApplicationVersion);
    
        return modelApplicationVersion;
    }
    
    public ApplicationVersionSearchModel getApplicationVersionSearchModel() {
        if (modelApplicationVersionSearch != null) return modelApplicationVersionSearch;
        modelApplicationVersionSearch = new ApplicationVersionSearchModel();
        HubSelectDelegate.adoptWhereHub(modelApplicationVersionSearch.getHub(), VersionFile.P_ApplicationVersion, getHub());
        return modelApplicationVersionSearch;
    }
    
    public HubCopy<VersionFile> createHubCopy() {
        Hub<VersionFile> hubVersionFilex = new Hub<>(VersionFile.class);
        HubCopy<VersionFile> hc = new HubCopy<>(getHub(), hubVersionFilex, true);
        return hc;
    }
    public VersionFileModel createCopy() {
        VersionFileModel mod = new VersionFileModel(createHubCopy().getHub());
        return mod;
    }
}

