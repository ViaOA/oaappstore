// Generated by OABuilder

package com.viaoa.appstore.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.viaoa.appstore.model.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.appstore.model.oa.search.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.delegate.ModelDelegate;
import com.viaoa.appstore.resource.Resource;

public class RunningAppSearchModel {
    private static Logger LOG = Logger.getLogger(RunningAppSearchModel.class.getName());
    
    protected Hub<RunningApp> hub;  // search results
    protected Hub<RunningApp> hubMultiSelect;
    protected Hub<RunningApp> hubSearchFrom;  // hub (optional) to search from
    protected Hub<RunningAppSearch> hubRunningAppSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, RunningApp> finder;
    
    // object used for search data
    protected RunningAppSearch runningAppSearch;
    
    public RunningAppSearchModel() {
    }
    
    public RunningAppSearchModel(Hub<RunningApp> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<RunningApp> getHub() {
        if (hub == null) {
            hub = new Hub<RunningApp>(RunningApp.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<RunningApp> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<RunningApp> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    RunningAppSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<RunningApp> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(RunningApp.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, RunningApp> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, RunningApp> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public RunningAppSearch getRunningAppSearch() {
        if (runningAppSearch != null) return runningAppSearch;
        runningAppSearch = new RunningAppSearch();
        return runningAppSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<RunningAppSearch> getRunningAppSearchHub() {
        if (hubRunningAppSearch == null) {
            hubRunningAppSearch = new Hub<RunningAppSearch>(RunningAppSearch.class);
            hubRunningAppSearch.add(getRunningAppSearch());
            hubRunningAppSearch.setPos(0);
        }
        return hubRunningAppSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses RunningAppSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<RunningApp> sel = getRunningAppSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(RunningApp runningApp, Hub<RunningApp> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<RunningApp> hub) {
    }
}

