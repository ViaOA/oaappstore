// Generated by OABuilder

package com.viaoa.appstore.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.viaoa.appstore.model.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.appstore.model.oa.search.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.delegate.ModelDelegate;
import com.viaoa.appstore.resource.Resource;

public class ServerSearchModel {
    private static Logger LOG = Logger.getLogger(ServerSearchModel.class.getName());
    
    protected Hub<Server> hub;  // search results
    protected Hub<Server> hubMultiSelect;
    protected Hub<Server> hubSearchFrom;  // hub (optional) to search from
    protected Hub<ServerSearch> hubServerSearch;  // search data, size=1, AO
    // references used in search
    protected Hub<Environment> hubEnvironment;
    protected Hub<ApplicationType> hubApplicationsApplicationType;
    
    // finder used to find objects in a path
    protected OAFinder<?, Server> finder;
    
    // ObjectModels
    protected EnvironmentModel modelEnvironment;
    protected ApplicationTypeModel modelApplicationsApplicationType;
    
    // SearchModels
    protected EnvironmentSearchModel modelEnvironmentSearch;
    protected ApplicationTypeSearchModel modelApplicationsApplicationTypeSearch;
    
    // object used for search data
    protected ServerSearch serverSearch;
    
    public ServerSearchModel() {
    }
    
    public ServerSearchModel(Hub<Server> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<Server> getHub() {
        if (hub == null) {
            hub = new Hub<Server>(Server.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<Server> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<Server> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    ServerSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<Server> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(Server.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, Server> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, Server> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public ServerSearch getServerSearch() {
        if (serverSearch != null) return serverSearch;
        serverSearch = new ServerSearch();
        return serverSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<ServerSearch> getServerSearchHub() {
        if (hubServerSearch == null) {
            hubServerSearch = new Hub<ServerSearch>(ServerSearch.class);
            hubServerSearch.add(getServerSearch());
            hubServerSearch.setPos(0);
        }
        return hubServerSearch;
    }
    public Hub<Environment> getEnvironmentHub() {
        if (hubEnvironment != null) return hubEnvironment;
        hubEnvironment = new Hub<>(Environment.class);
        Hub<Environment> hub = ModelDelegate.getEnvironments();
        HubCopy<Environment> hc = new HubCopy<>(hub, hubEnvironment, false);
        hubEnvironment.setLinkHub(getServerSearchHub(), ServerSearch.P_Environment); 
        return hubEnvironment;
    }
    public Hub<ApplicationType> getApplicationsApplicationTypeHub() {
        if (hubApplicationsApplicationType != null) return hubApplicationsApplicationType;
        hubApplicationsApplicationType = new Hub<>(ApplicationType.class);
        Hub<ApplicationType> hub = ModelDelegate.getApplicationTypes();
        HubCopy<ApplicationType> hc = new HubCopy<>(hub, hubApplicationsApplicationType, false);
        hubApplicationsApplicationType.setLinkHub(getServerSearchHub(), ServerSearch.P_ApplicationsApplicationType); 
        return hubApplicationsApplicationType;
    }
    
    public EnvironmentModel getEnvironmentModel() {
        if (modelEnvironment != null) return modelEnvironment;
        modelEnvironment = new EnvironmentModel(getEnvironmentHub());
        modelEnvironment.setDisplayName("Environment");
        modelEnvironment.setPluralDisplayName("Environments");
        modelEnvironment.setAllowNew(false);
        modelEnvironment.setAllowSave(true);
        modelEnvironment.setAllowAdd(false);
        modelEnvironment.setAllowRemove(false);
        modelEnvironment.setAllowClear(true);
        modelEnvironment.setAllowDelete(false);
        modelEnvironment.setAllowSearch(true);
        modelEnvironment.setAllowHubSearch(false);
        modelEnvironment.setAllowGotoEdit(true);
        return modelEnvironment;
    }
    public ApplicationTypeModel getApplicationsApplicationTypeModel() {
        if (modelApplicationsApplicationType != null) return modelApplicationsApplicationType;
        modelApplicationsApplicationType = new ApplicationTypeModel(getApplicationsApplicationTypeHub());
        modelApplicationsApplicationType.setDisplayName("Application Type");
        modelApplicationsApplicationType.setPluralDisplayName("Application Types");
        modelApplicationsApplicationType.setAllowNew(false);
        modelApplicationsApplicationType.setAllowSave(true);
        modelApplicationsApplicationType.setAllowAdd(false);
        modelApplicationsApplicationType.setAllowRemove(false);
        modelApplicationsApplicationType.setAllowClear(true);
        modelApplicationsApplicationType.setAllowDelete(false);
        modelApplicationsApplicationType.setAllowSearch(true);
        modelApplicationsApplicationType.setAllowHubSearch(false);
        modelApplicationsApplicationType.setAllowGotoEdit(true);
        return modelApplicationsApplicationType;
    }
    
    public EnvironmentSearchModel getEnvironmentSearchModel() {
        if (modelEnvironmentSearch == null) {
            modelEnvironmentSearch = new EnvironmentSearchModel();
            getServerSearch().setEnvironmentSearch(modelEnvironmentSearch.getEnvironmentSearch());
        }
        return modelEnvironmentSearch;
    }
    public ApplicationTypeSearchModel getApplicationsApplicationTypeSearchModel() {
        if (modelApplicationsApplicationTypeSearch == null) {
            modelApplicationsApplicationTypeSearch = new ApplicationTypeSearchModel();
            getServerSearch().setApplicationsApplicationTypeSearch(modelApplicationsApplicationTypeSearch.getApplicationTypeSearch());
        }
        return modelApplicationsApplicationTypeSearch;
    }
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses ServerSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<Server> sel = getServerSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(Server server, Hub<Server> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<Server> hub) {
    }
}

