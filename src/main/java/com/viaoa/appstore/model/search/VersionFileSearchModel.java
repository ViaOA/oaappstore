// Generated by OABuilder

package com.viaoa.appstore.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.viaoa.appstore.model.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.appstore.model.oa.search.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.delegate.ModelDelegate;
import com.viaoa.appstore.resource.Resource;

public class VersionFileSearchModel {
    private static Logger LOG = Logger.getLogger(VersionFileSearchModel.class.getName());
    
    protected Hub<VersionFile> hub;  // search results
    protected Hub<VersionFile> hubMultiSelect;
    protected Hub<VersionFile> hubSearchFrom;  // hub (optional) to search from
    protected Hub<VersionFileSearch> hubVersionFileSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, VersionFile> finder;
    
    // object used for search data
    protected VersionFileSearch versionFileSearch;
    
    public VersionFileSearchModel() {
    }
    
    public VersionFileSearchModel(Hub<VersionFile> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<VersionFile> getHub() {
        if (hub == null) {
            hub = new Hub<VersionFile>(VersionFile.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<VersionFile> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<VersionFile> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    VersionFileSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<VersionFile> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(VersionFile.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, VersionFile> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, VersionFile> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public VersionFileSearch getVersionFileSearch() {
        if (versionFileSearch != null) return versionFileSearch;
        versionFileSearch = new VersionFileSearch();
        return versionFileSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<VersionFileSearch> getVersionFileSearchHub() {
        if (hubVersionFileSearch == null) {
            hubVersionFileSearch = new Hub<VersionFileSearch>(VersionFileSearch.class);
            hubVersionFileSearch.add(getVersionFileSearch());
            hubVersionFileSearch.setPos(0);
        }
        return hubVersionFileSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses VersionFileSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<VersionFile> sel = getVersionFileSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(VersionFile versionFile, Hub<VersionFile> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<VersionFile> hub) {
    }
}

