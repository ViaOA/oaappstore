// Generated by OABuilder

package com.viaoa.appstore.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.viaoa.appstore.model.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.appstore.model.oa.search.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.delegate.ModelDelegate;
import com.viaoa.appstore.resource.Resource;

public class AppStoreServerSearchModel {
    private static Logger LOG = Logger.getLogger(AppStoreServerSearchModel.class.getName());
    
    protected Hub<AppStoreServer> hub;  // search results
    protected Hub<AppStoreServer> hubMultiSelect;
    protected Hub<AppStoreServer> hubSearchFrom;  // hub (optional) to search from
    protected Hub<AppStoreServerSearch> hubAppStoreServerSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, AppStoreServer> finder;
    
    // object used for search data
    protected AppStoreServerSearch appStoreServerSearch;
    
    public AppStoreServerSearchModel() {
    }
    
    public AppStoreServerSearchModel(Hub<AppStoreServer> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<AppStoreServer> getHub() {
        if (hub == null) {
            hub = new Hub<AppStoreServer>(AppStoreServer.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<AppStoreServer> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<AppStoreServer> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    AppStoreServerSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<AppStoreServer> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(AppStoreServer.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, AppStoreServer> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, AppStoreServer> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public AppStoreServerSearch getAppStoreServerSearch() {
        if (appStoreServerSearch != null) return appStoreServerSearch;
        appStoreServerSearch = new AppStoreServerSearch();
        return appStoreServerSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<AppStoreServerSearch> getAppStoreServerSearchHub() {
        if (hubAppStoreServerSearch == null) {
            hubAppStoreServerSearch = new Hub<AppStoreServerSearch>(AppStoreServerSearch.class);
            hubAppStoreServerSearch.add(getAppStoreServerSearch());
            hubAppStoreServerSearch.setPos(0);
        }
        return hubAppStoreServerSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses AppStoreServerSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<AppStoreServer> sel = getAppStoreServerSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(AppStoreServer appStoreServer, Hub<AppStoreServer> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<AppStoreServer> hub) {
    }
}

