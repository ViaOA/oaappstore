// Generated by OABuilder

package com.viaoa.appstore.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.viaoa.appstore.model.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.appstore.model.oa.search.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.delegate.ModelDelegate;
import com.viaoa.appstore.resource.Resource;

public class ApplicationTypeSearchModel {
    private static Logger LOG = Logger.getLogger(ApplicationTypeSearchModel.class.getName());
    
    protected Hub<ApplicationType> hub;  // search results
    protected Hub<ApplicationType> hubMultiSelect;
    protected Hub<ApplicationType> hubSearchFrom;  // hub (optional) to search from
    protected Hub<ApplicationTypeSearch> hubApplicationTypeSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, ApplicationType> finder;
    
    // object used for search data
    protected ApplicationTypeSearch applicationTypeSearch;
    
    public ApplicationTypeSearchModel() {
    }
    
    public ApplicationTypeSearchModel(Hub<ApplicationType> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<ApplicationType> getHub() {
        if (hub == null) {
            hub = new Hub<ApplicationType>(ApplicationType.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<ApplicationType> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<ApplicationType> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    ApplicationTypeSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<ApplicationType> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(ApplicationType.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, ApplicationType> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, ApplicationType> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public ApplicationTypeSearch getApplicationTypeSearch() {
        if (applicationTypeSearch != null) return applicationTypeSearch;
        applicationTypeSearch = new ApplicationTypeSearch();
        return applicationTypeSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<ApplicationTypeSearch> getApplicationTypeSearchHub() {
        if (hubApplicationTypeSearch == null) {
            hubApplicationTypeSearch = new Hub<ApplicationTypeSearch>(ApplicationTypeSearch.class);
            hubApplicationTypeSearch.add(getApplicationTypeSearch());
            hubApplicationTypeSearch.setPos(0);
        }
        return hubApplicationTypeSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses ApplicationTypeSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<ApplicationType> sel = getApplicationTypeSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(ApplicationType applicationType, Hub<ApplicationType> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<ApplicationType> hub) {
    }
}

