// Generated by OABuilder

package com.viaoa.appstore.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.viaoa.appstore.model.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.appstore.model.oa.search.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.delegate.ModelDelegate;
import com.viaoa.appstore.resource.Resource;

public class SingleAppSearchModel {
    private static Logger LOG = Logger.getLogger(SingleAppSearchModel.class.getName());
    
    protected Hub<SingleApp> hub;  // search results
    protected Hub<SingleApp> hubMultiSelect;
    protected Hub<SingleApp> hubSearchFrom;  // hub (optional) to search from
    protected Hub<SingleAppSearch> hubSingleAppSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, SingleApp> finder;
    
    // object used for search data
    protected SingleAppSearch singleAppSearch;
    
    public SingleAppSearchModel() {
    }
    
    public SingleAppSearchModel(Hub<SingleApp> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<SingleApp> getHub() {
        if (hub == null) {
            hub = new Hub<SingleApp>(SingleApp.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<SingleApp> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<SingleApp> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    SingleAppSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<SingleApp> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(SingleApp.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, SingleApp> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, SingleApp> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public SingleAppSearch getSingleAppSearch() {
        if (singleAppSearch != null) return singleAppSearch;
        singleAppSearch = new SingleAppSearch();
        return singleAppSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<SingleAppSearch> getSingleAppSearchHub() {
        if (hubSingleAppSearch == null) {
            hubSingleAppSearch = new Hub<SingleAppSearch>(SingleAppSearch.class);
            hubSingleAppSearch.add(getSingleAppSearch());
            hubSingleAppSearch.setPos(0);
        }
        return hubSingleAppSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses SingleAppSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<SingleApp> sel = getSingleAppSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(SingleApp singleApp, Hub<SingleApp> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<SingleApp> hub) {
    }
}

