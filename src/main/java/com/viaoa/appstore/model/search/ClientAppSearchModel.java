// Generated by OABuilder

package com.viaoa.appstore.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.viaoa.appstore.model.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.appstore.model.oa.search.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.delegate.ModelDelegate;
import com.viaoa.appstore.resource.Resource;

public class ClientAppSearchModel {
    private static Logger LOG = Logger.getLogger(ClientAppSearchModel.class.getName());
    
    protected Hub<ClientApp> hub;  // search results
    protected Hub<ClientApp> hubMultiSelect;
    protected Hub<ClientApp> hubSearchFrom;  // hub (optional) to search from
    protected Hub<ClientAppSearch> hubClientAppSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, ClientApp> finder;
    
    // object used for search data
    protected ClientAppSearch clientAppSearch;
    
    public ClientAppSearchModel() {
    }
    
    public ClientAppSearchModel(Hub<ClientApp> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<ClientApp> getHub() {
        if (hub == null) {
            hub = new Hub<ClientApp>(ClientApp.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<ClientApp> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<ClientApp> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    ClientAppSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<ClientApp> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(ClientApp.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, ClientApp> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, ClientApp> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public ClientAppSearch getClientAppSearch() {
        if (clientAppSearch != null) return clientAppSearch;
        clientAppSearch = new ClientAppSearch();
        return clientAppSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<ClientAppSearch> getClientAppSearchHub() {
        if (hubClientAppSearch == null) {
            hubClientAppSearch = new Hub<ClientAppSearch>(ClientAppSearch.class);
            hubClientAppSearch.add(getClientAppSearch());
            hubClientAppSearch.setPos(0);
        }
        return hubClientAppSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses ClientAppSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<ClientApp> sel = getClientAppSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(ClientApp clientApp, Hub<ClientApp> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<ClientApp> hub) {
    }
}

