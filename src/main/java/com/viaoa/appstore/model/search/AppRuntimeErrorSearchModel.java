// Generated by OABuilder

package com.viaoa.appstore.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.viaoa.appstore.model.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.appstore.model.oa.search.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.delegate.ModelDelegate;
import com.viaoa.appstore.resource.Resource;

public class AppRuntimeErrorSearchModel {
    private static Logger LOG = Logger.getLogger(AppRuntimeErrorSearchModel.class.getName());
    
    protected Hub<AppRuntimeError> hub;  // search results
    protected Hub<AppRuntimeError> hubMultiSelect;
    protected Hub<AppRuntimeError> hubSearchFrom;  // hub (optional) to search from
    protected Hub<AppRuntimeErrorSearch> hubAppRuntimeErrorSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, AppRuntimeError> finder;
    
    // object used for search data
    protected AppRuntimeErrorSearch appRuntimeErrorSearch;
    
    public AppRuntimeErrorSearchModel() {
    }
    
    public AppRuntimeErrorSearchModel(Hub<AppRuntimeError> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<AppRuntimeError> getHub() {
        if (hub == null) {
            hub = new Hub<AppRuntimeError>(AppRuntimeError.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<AppRuntimeError> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<AppRuntimeError> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    AppRuntimeErrorSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<AppRuntimeError> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(AppRuntimeError.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, AppRuntimeError> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, AppRuntimeError> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public AppRuntimeErrorSearch getAppRuntimeErrorSearch() {
        if (appRuntimeErrorSearch != null) return appRuntimeErrorSearch;
        appRuntimeErrorSearch = new AppRuntimeErrorSearch();
        return appRuntimeErrorSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<AppRuntimeErrorSearch> getAppRuntimeErrorSearchHub() {
        if (hubAppRuntimeErrorSearch == null) {
            hubAppRuntimeErrorSearch = new Hub<AppRuntimeErrorSearch>(AppRuntimeErrorSearch.class);
            hubAppRuntimeErrorSearch.add(getAppRuntimeErrorSearch());
            hubAppRuntimeErrorSearch.setPos(0);
        }
        return hubAppRuntimeErrorSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses AppRuntimeErrorSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<AppRuntimeError> sel = getAppRuntimeErrorSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(AppRuntimeError appRuntimeError, Hub<AppRuntimeError> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<AppRuntimeError> hub) {
    }
}

