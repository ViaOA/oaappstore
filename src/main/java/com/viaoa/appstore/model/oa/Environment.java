// Generated by OABuilder
package com.viaoa.appstore.model.oa;
 
import java.util.*;
import java.util.logging.*;
import java.sql.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.scheduler.*;
import com.viaoa.util.*;
import com.viaoa.annotation.*;
import com.viaoa.util.OADateTime;
import com.viaoa.appstore.delegate.oa.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.model.oa.propertypath.*;
 
@OAClass(
    lowerName = "environment",
    pluralName = "Environments",
    shortName = "env",
    displayName = "Environment",
    isLookup = true,
    isPreSelect = true,
    displayProperty = "name",
    sortProperty = "seq",
    noPojo = true
)
@OATable(
)
public class Environment extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(Environment.class.getName());

    public static final String P_Id = "id";
    public static final String P_Created = "created";
    public static final String P_Name = "name";
    public static final String P_AbbrevName = "abbrevName";
    public static final String P_Type = "type";
    public static final String P_TypeString = "typeString";
    public static final String P_TypeEnum = "typeEnum";
    public static final String P_TypeDisplay = "typeDisplay";
    public static final String P_Seq = "seq";
     
    public static final String P_MergeServerApplications = "mergeServerApplications";
    public static final String P_Servers = "servers";
     
    protected volatile int id;
    protected volatile OADateTime created;
    protected volatile String name;
    protected volatile String abbrevName;
    protected volatile int type;

    public static enum Type {
        Unknown("Unknown"),
        User("User"),
        Development("Development"),
        Test("Test"),
        QA("QA"),
        Staging("Staging"),
        UAT("UAT"),
        Production("Production"),
        Other("Other");

        private String display;
        Type(String display) {
            this.display = display;
        }

        public String getDisplay() {
            return display;
        }
    }
    public static final int TYPE_Unknown = 0;
    public static final int TYPE_User = 1;
    public static final int TYPE_Development = 2;
    public static final int TYPE_Test = 3;
    public static final int TYPE_QA = 4;
    public static final int TYPE_Staging = 5;
    public static final int TYPE_UAT = 6;
    public static final int TYPE_Production = 7;
    public static final int TYPE_Other = 8;

    protected volatile int seq;
     
    // Links to other objects.
    protected transient Hub<ServerApplication> hubMergeServerApplications;
    protected transient Hub<Server> hubServers;
     
    public Environment() {
        if (!isLoading()) setObjectDefaults();
    }
    @Override
    public void setObjectDefaults() {
        setCreated(new OADateTime());
    }
     
    public Environment(int id) {
        this();
        setId(id);
    }
    @OAObjCallback(contextEnabledProperty = AppUser.P_Admin)
    public void callback(final OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        }
    }

    @OAProperty(isUnique = true, displayLength = 6)
    @OAId
    @OAColumn(name = "Id", sqlType = java.sql.Types.INTEGER)
    public int getId() {
        return id;
    }
    public void setId(int newValue) {
        int old = id;
        fireBeforePropertyChange(P_Id, old, newValue);
        this.id = newValue;
        firePropertyChange(P_Id, old, this.id);
    }

    @OAProperty(defaultValue = "new OADateTime()", displayLength = 15, isProcessed = true)
    @OAColumn(name = "Created", sqlType = java.sql.Types.TIMESTAMP)
    public OADateTime getCreated() {
        return created;
    }
    public void setCreated(OADateTime newValue) {
        OADateTime old = created;
        fireBeforePropertyChange(P_Created, old, newValue);
        this.created = newValue;
        firePropertyChange(P_Created, old, this.created);
    }

    @OAProperty(maxLength = 35, displayLength = 15)
    @OAColumn(name = "Name", maxLength = 35)
    public String getName() {
        return name;
    }
    public void setName(String newValue) {
        String old = name;
        fireBeforePropertyChange(P_Name, old, newValue);
        this.name = newValue;
        firePropertyChange(P_Name, old, this.name);
    }

    @OAProperty(displayName = "Abbrev Name", maxLength = 8, displayLength = 5, uiColumnLength = 6, uiColumnName = "Abbrev")
    @OAColumn(name = "AbbrevName", maxLength = 8)
    public String getAbbrevName() {
        return abbrevName;
    }
    public void setAbbrevName(String newValue) {
        String old = abbrevName;
        fireBeforePropertyChange(P_AbbrevName, old, newValue);
        this.abbrevName = newValue;
        firePropertyChange(P_AbbrevName, old, this.abbrevName);
    }

    @OAProperty(displayLength = 16, isNameValue = true)
    @OAColumn(name = "Type", sqlType = java.sql.Types.INTEGER)
    public int getType() {
        return type;
    }
    public void setType(int newValue) {
        int old = type;
        fireBeforePropertyChange(P_Type, old, newValue);
        this.type = newValue;
        firePropertyChange(P_Type, old, this.type);
    }

    @OAProperty(enumPropertyName = P_Type)
    public String getTypeString() {
        Type type = getTypeEnum();
        if (type == null) return null;
        return type.name();
    }
    public void setTypeString(String val) {
        int x = -1;
        if (OAString.isNotEmpty(val)) {
            Type type = Type.valueOf(val);
            if (type != null) x = type.ordinal();
        }
        if (x < 0) setNull(P_Type);
        else setType(x);
    }
    @OAProperty(enumPropertyName = P_Type)
    public Type getTypeEnum() {
        if (isNull(P_Type)) return null;
        final int val = getType();
        if (val < 0 || val >= Type.values().length) return null;
        return Type.values()[val];
    }
    public void setTypeEnum(Type val) {
        if (val == null) {
            setNull(P_Type);
        }
        else {
            setType(val.ordinal());
        }
    }
    @OACalculatedProperty(enumPropertyName = P_Type, displayName = "Type", displayLength = 16, columnLength = 16, properties = {P_Type} )
    public String getTypeDisplay() {
        Type type = getTypeEnum();
        if (type == null) return null;
        return type.getDisplay();
    }

    @OAProperty(displayLength = 6, isAutoSeq = true)
    @OAColumn(name = "Seq", sqlType = java.sql.Types.INTEGER)
    public int getSeq() {
        return seq;
    }
    public void setSeq(int newValue) {
        int old = seq;
        fireBeforePropertyChange(P_Seq, old, newValue);
        this.seq = newValue;
        firePropertyChange(P_Seq, old, this.seq);
    }

    @OAMany(
        displayName = "Server Applications", 
        toClass = ServerApplication.class, 
        isCalculated = true, 
        mergerPropertyPath = Environment.P_Servers+"."+Server.P_ServerApplications, 
        reverseName = ServerApplication.P_MergeEnvironment
    )
    public Hub<ServerApplication> getMergeServerApplications() {
        if (hubMergeServerApplications != null) return hubMergeServerApplications;
        hubMergeServerApplications = (Hub<ServerApplication>) getHub(P_MergeServerApplications);
        return hubMergeServerApplications;
    }

    @OAMany(
        toClass = Server.class, 
        owner = true, 
        reverseName = Server.P_Environment, 
        cascadeSave = true, 
        cascadeDelete = true
    )
    public Hub<Server> getServers() {
        if (hubServers == null) {
            hubServers = (Hub<Server>) getHub(P_Servers);
        }
        return hubServers;
    }
    public void load(ResultSet rs, int id) throws SQLException {
        this.id = id;
        java.sql.Timestamp timestamp;
        timestamp = rs.getTimestamp(2);
        if (timestamp != null) this.created = new OADateTime(timestamp);
        this.name = rs.getString(3);
        this.abbrevName = rs.getString(4);
        this.type = rs.getInt(5);
        OAObjectInfoDelegate.setPrimitiveNull(this, P_Type, rs.wasNull());
        this.seq = rs.getInt(6);
        OAObjectInfoDelegate.setPrimitiveNull(this, P_Seq, rs.wasNull());

        this.changedFlag = false;
        this.newFlag = false;
    }
}
 
