// Generated by OABuilder
package com.viaoa.appstore.model.oa;
 
import java.util.*;
import java.util.logging.*;
import java.sql.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.scheduler.*;
import com.viaoa.util.*;
import com.viaoa.annotation.*;
import com.viaoa.util.OADate;
import com.viaoa.appstore.delegate.oa.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.model.oa.propertypath.*;
 
@OAClass(
    lowerName = "appUser",
    pluralName = "AppUsers",
    shortName = "au",
    displayName = "App User",
    isLookup = true,
    isPreSelect = true,
    displayProperty = "displayName",
    noPojo = true
)
@OATable(
)
public class AppUser extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(AppUser.class.getName());

    public static final String P_Id = "id";
    public static final String P_LoginId = "loginId";
    public static final String P_Password = "password";
    public static final String P_Admin = "admin";
    public static final String P_SuperAdmin = "superAdmin";
    public static final String P_EditProcessed = "editProcessed";
    public static final String P_FirstName = "firstName";
    public static final String P_LastName = "lastName";
    public static final String P_InactiveDate = "inactiveDate";
    public static final String P_Note = "note";
    public static final String P_DisableAutoLogin = "disableAutoLogin";
     
    public static final String P_FullName = "fullName";
    public static final String P_DisplayName = "displayName";
    public static final String P_CalcCheckVersions = "calcCheckVersions";
     
    public static final String P_ApplicationTypes = "applicationTypes";
    public static final String P_ApplicationTypesId = "applicationTypesId"; // fkey
    public static final String P_AppUserLogins = "appUserLogins";
    public static final String P_ClientApps = "clientApps";
    public static final String P_MergeServerApplications = "mergeServerApplications";
    public static final String P_Server = "server";
    public static final String P_SingleApps = "singleApps";
     
    protected volatile int id;
    protected volatile String loginId;
    protected volatile String password;
    protected volatile boolean admin;
    protected volatile boolean superAdmin;
    protected volatile boolean editProcessed;
    protected volatile String firstName;
    protected volatile String lastName;
    protected volatile OADate inactiveDate;
    protected volatile String note;
    protected volatile boolean disableAutoLogin;
     
    // Links to other objects.
    protected transient Hub<ApplicationType> hubApplicationTypes;
    protected transient Hub<AppUserLogin> hubAppUserLogins;
    protected transient Hub<ClientApp> hubClientApps;
    protected transient Hub<ServerApplication> hubMergeServerApplications;
    protected volatile transient Server server;
    protected transient Hub<SingleApp> hubSingleApps;
     
    public AppUser() {
        if (!isLoading()) setObjectDefaults();
    }
     
    public AppUser(int id) {
        this();
        setId(id);
    }
    @OAObjCallback(contextEnabledProperty = AppUser.P_Admin)
    public void callback(final OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        }
    }

    @OAProperty(isUnique = true, displayLength = 5)
    @OAId
    @OAColumn(name = "Id", sqlType = java.sql.Types.INTEGER)
    public int getId() {
        return id;
    }
    public void setId(int newValue) {
        int old = id;
        fireBeforePropertyChange(P_Id, old, newValue);
        this.id = newValue;
        firePropertyChange(P_Id, old, this.id);
    }

    @OAProperty(displayName = "Login Id", maxLength = 24, displayLength = 12)
    @OAColumn(name = "LoginId", maxLength = 24)
    public String getLoginId() {
        return loginId;
    }
    public void setLoginId(String newValue) {
        String old = loginId;
        fireBeforePropertyChange(P_LoginId, old, newValue);
        this.loginId = newValue;
        firePropertyChange(P_LoginId, old, this.loginId);
    }

    @OAProperty(maxLength = 50, displayLength = 12, uiColumnLength = 10, isSHAHash = true)
    @OAColumn(name = "Password", maxLength = 50)
    public String getPassword() {
        return password;
    }
    public void setPassword(String newValue) {
        String old = password;
        fireBeforePropertyChange(P_Password, old, newValue);
        this.password = newValue;
        firePropertyChange(P_Password, old, this.password);
    }

    @OAProperty(displayLength = 5)
    @OAColumn(name = "Admin", sqlType = java.sql.Types.BOOLEAN)
    public boolean getAdmin() {
        return admin;
    }
    public boolean isAdmin() {
        return getAdmin();
    }
    public void setAdmin(boolean newValue) {
        boolean old = admin;
        fireBeforePropertyChange(P_Admin, old, newValue);
        this.admin = newValue;
        firePropertyChange(P_Admin, old, this.admin);
    }

    @OAProperty(displayName = "Super Admin", displayLength = 5, uiColumnLength = 11)
    public boolean getSuperAdmin() {
        return superAdmin;
    }
    public boolean isSuperAdmin() {
        return getSuperAdmin();
    }
    public void setSuperAdmin(boolean newValue) {
        boolean old = superAdmin;
        fireBeforePropertyChange(P_SuperAdmin, old, newValue);
        this.superAdmin = newValue;
        firePropertyChange(P_SuperAdmin, old, this.superAdmin);
    }
     
    @OAObjCallback(contextEnabledProperty = AppUser.P_Admin)
    public void superAdminCallback(OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        }
    }

    @OAProperty(displayName = "Edit Processed", displayLength = 5)
    @OAColumn(name = "EditProcessed", sqlType = java.sql.Types.BOOLEAN)
    public boolean getEditProcessed() {
        return editProcessed;
    }
    public boolean isEditProcessed() {
        return getEditProcessed();
    }
    public void setEditProcessed(boolean newValue) {
        boolean old = editProcessed;
        fireBeforePropertyChange(P_EditProcessed, old, newValue);
        this.editProcessed = newValue;
        firePropertyChange(P_EditProcessed, old, this.editProcessed);
    }

    @OAProperty(displayName = "First Name", maxLength = 30, displayLength = 12)
    @OAColumn(name = "FirstName", maxLength = 30)
    public String getFirstName() {
        return firstName;
    }
    public void setFirstName(String newValue) {
        String old = firstName;
        fireBeforePropertyChange(P_FirstName, old, newValue);
        this.firstName = newValue;
        firePropertyChange(P_FirstName, old, this.firstName);
    }

    @OAProperty(displayName = "Last Name", maxLength = 55, displayLength = 12)
    @OAColumn(name = "LastName", maxLength = 55)
    public String getLastName() {
        return lastName;
    }
    public void setLastName(String newValue) {
        String old = lastName;
        fireBeforePropertyChange(P_LastName, old, newValue);
        this.lastName = newValue;
        firePropertyChange(P_LastName, old, this.lastName);
    }

    @OAProperty(displayName = "Inactive Date", displayLength = 8)
    @OAColumn(name = "InactiveDate", sqlType = java.sql.Types.DATE)
    public OADate getInactiveDate() {
        return inactiveDate;
    }
    public void setInactiveDate(OADate newValue) {
        OADate old = inactiveDate;
        fireBeforePropertyChange(P_InactiveDate, old, newValue);
        this.inactiveDate = newValue;
        firePropertyChange(P_InactiveDate, old, this.inactiveDate);
    }

    @OAProperty(displayLength = 20, uiColumnLength = 15)
    @OAColumn(name = "Note", sqlType = java.sql.Types.CLOB)
    public String getNote() {
        return note;
    }
    public void setNote(String newValue) {
        String old = note;
        fireBeforePropertyChange(P_Note, old, newValue);
        this.note = newValue;
        firePropertyChange(P_Note, old, this.note);
    }

    @OAProperty(displayName = "Disable Auto Login", displayLength = 5, uiColumnLength = 18)
    @OAColumn(name = "DisableAutoLogin", sqlType = java.sql.Types.BOOLEAN)
    public boolean getDisableAutoLogin() {
        return disableAutoLogin;
    }
    public boolean isDisableAutoLogin() {
        return getDisableAutoLogin();
    }
    public void setDisableAutoLogin(boolean newValue) {
        boolean old = disableAutoLogin;
        fireBeforePropertyChange(P_DisableAutoLogin, old, newValue);
        this.disableAutoLogin = newValue;
        firePropertyChange(P_DisableAutoLogin, old, this.disableAutoLogin);
    }
    @OACalculatedProperty(displayName = "Full Name", displayLength = 23, columnLength = 16, properties = {P_FirstName, P_LastName})
    public String getFullName() {
        String fullName = "";
        // firstName
        firstName = this.getFirstName();
        if (firstName != null) {
            if (fullName.length() > 0) fullName += " ";
            fullName += firstName;
        }
    
        // lastName
        lastName = this.getLastName();
        if (lastName != null) {
            if (fullName.length() > 0) fullName += " ";
            fullName += lastName;
        }
    
        return fullName;
    }
    @OACalculatedProperty(displayName = "Display Name", displayLength = 28, columnLength = 22, properties = {P_LoginId, P_FullName})
    public String getDisplayName() {
        String displayName = "";
        loginId = this.getLoginId();
        if (loginId != null) {
            displayName = loginId;
        }
        if (displayName.length() > 0) displayName += " ";
        displayName += " (";
        String fullName = this.getFullName();
        if (fullName != null) {
            displayName += fullName;
        }
        displayName += ")";
        return displayName;
    }
    @OACalculatedProperty(displayName = "Calc Check Versions", description = "calc prop that is triggered whenever a users applications need to be rechecked", displayLength = 5, columnLength = 19, properties = {P_ApplicationTypes+"."+ApplicationType.P_ClientPort, P_ApplicationTypes+"."+ApplicationType.P_ServerApplications+"."+ServerApplication.P_ClientPort, P_ApplicationTypes+"."+ApplicationType.P_ServerApplications+"."+ServerApplication.P_Server+"."+Server.P_Host, P_ApplicationTypes+"."+ApplicationType.P_ServerApplications+"."+ServerApplication.P_Server+"."+Server.P_IpAddress, P_ApplicationTypes+"."+ApplicationType.P_DirectoryName})
    /**
      calc prop that is triggered whenever a users applications need to be rechecked
    */
    public boolean getCalcCheckVersions() {
        // no-op used to for listener to know when any of a user's applications has changed.
        return false;
    }

    @OAMany(
        displayName = "Application Types", 
        toClass = ApplicationType.class, 
        reverseName = ApplicationType.P_AppUsers
    )
    @OALinkTable(name = "AppUserApplicationType", indexName = "ApplicationTypeAppUser", columns = {"AppUserId"})
    public Hub<ApplicationType> getApplicationTypes() {
        if (hubApplicationTypes == null) {
            hubApplicationTypes = (Hub<ApplicationType>) getHub(P_ApplicationTypes);
        }
        return hubApplicationTypes;
    }

    @OAMany(
        displayName = "App User Logins", 
        toClass = AppUserLogin.class, 
        owner = true, 
        reverseName = AppUserLogin.P_AppUser, 
        cascadeSave = true, 
        cascadeDelete = true
    )
    public Hub<AppUserLogin> getAppUserLogins() {
        if (hubAppUserLogins == null) {
            hubAppUserLogins = (Hub<AppUserLogin>) getHub(P_AppUserLogins);
        }
        return hubAppUserLogins;
    }

    @OAMany(
        displayName = "Client Apps", 
        toClass = ClientApp.class, 
        owner = true, 
        reverseName = ClientApp.P_AppUser, 
        cascadeSave = true, 
        cascadeDelete = true
    )
    public Hub<ClientApp> getClientApps() {
        if (hubClientApps == null) {
            hubClientApps = (Hub<ClientApp>) getHub(P_ClientApps);
        }
        return hubClientApps;
    }

    @OAMany(
        displayName = "Server Applications", 
        toClass = ServerApplication.class, 
        isCalculated = true, 
        mergerPropertyPath = AppUser.P_ApplicationTypes+"."+ApplicationType.P_ServerApplications, 
        reverseName = ServerApplication.P_MergeAppUsers
    )
    public Hub<ServerApplication> getMergeServerApplications() {
        if (hubMergeServerApplications != null) return hubMergeServerApplications;
        hubMergeServerApplications = (Hub<ServerApplication>) getHub(P_MergeServerApplications);
        return hubMergeServerApplications;
    }

    @OAOne(
        reverseName = Server.P_AppUser, 
        allowCreateNew = false
    )
    public Server getServer() {
        if (server == null) {
            server = (Server) getObject(P_Server);
        }
        return server;
    }
    public void setServer(Server newValue) {
        Server old = this.server;
        fireBeforePropertyChange(P_Server, old, newValue);
        this.server = newValue;
        firePropertyChange(P_Server, old, this.server);
    }

    @OAMany(
        displayName = "Single Apps", 
        toClass = SingleApp.class, 
        owner = true, 
        reverseName = SingleApp.P_AppUser, 
        cascadeSave = true, 
        cascadeDelete = true
    )
    public Hub<SingleApp> getSingleApps() {
        if (hubSingleApps == null) {
            hubSingleApps = (Hub<SingleApp>) getHub(P_SingleApps);
        }
        return hubSingleApps;
    }
    public void load(ResultSet rs, int id) throws SQLException {
        this.id = id;
        this.loginId = rs.getString(2);
        this.password = rs.getString(3);
        this.admin = rs.getBoolean(4);
        OAObjectInfoDelegate.setPrimitiveNull(this, P_Admin, rs.wasNull());
        this.editProcessed = rs.getBoolean(5);
        OAObjectInfoDelegate.setPrimitiveNull(this, P_EditProcessed, rs.wasNull());
        this.firstName = rs.getString(6);
        this.lastName = rs.getString(7);
        java.sql.Date date;
        date = rs.getDate(8);
        if (date != null) this.inactiveDate = new OADate(date);
        this.note = rs.getString(9);
        this.disableAutoLogin = rs.getBoolean(10);
        OAObjectInfoDelegate.setPrimitiveNull(this, P_DisableAutoLogin, rs.wasNull());

        this.changedFlag = false;
        this.newFlag = false;
    }
}
 
