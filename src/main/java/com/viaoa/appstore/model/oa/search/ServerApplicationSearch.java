// Generated by OABuilder
package com.viaoa.appstore.model.oa.search;

import java.util.*;
import java.util.logging.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.OADateTime;
import com.viaoa.datasource.*;
import com.viaoa.filter.*;

@OAClass(useDataSource=false, localOnly=true)
public class ServerApplicationSearch extends OAObject {
    private static final long serialVersionUID = 1L;

    private static Logger LOG = Logger.getLogger(ServerApplicationSearch.class.getName());

    public static final String P_ClientPort = "ClientPort";
    public static final String P_HttpPort = "HttpPort";
    public static final String P_HttpsPort = "HttpsPort";
    public static final String P_Id = "Id";
    public static final String P_Created = "Created";
    public static final String P_Created2 = "Created2";
    public static final String P_ApplicationType = "ApplicationType";
    public static final String P_UseApplicationTypeSearch = "UseApplicationTypeSearch";
    public static final String P_MaxResults = "MaxResults";

    protected int clientPort;
    protected int httpPort;
    protected int httpsPort;
    protected int id;
    protected OADateTime created;
    protected OADateTime created2;
    protected ApplicationType applicationType;
    protected boolean useApplicationTypeSearch;
    protected ApplicationTypeSearch searchApplicationType;
    protected int maxResults;

    @OAProperty(displayName = "Client Port", displayLength = 6, uiColumnLength = 11, format = "###")
    public int getClientPort() {
        return clientPort;
    }
    public void setClientPort(int newValue) {
        int old = clientPort;
        fireBeforePropertyChange(P_ClientPort, old, newValue);
        this.clientPort = newValue;
        firePropertyChange(P_ClientPort, old, this.clientPort);
    }
      
    @OAProperty(displayName = "Http Port", displayLength = 6, uiColumnLength = 9, format = "###")
    public int getHttpPort() {
        return httpPort;
    }
    public void setHttpPort(int newValue) {
        int old = httpPort;
        fireBeforePropertyChange(P_HttpPort, old, newValue);
        this.httpPort = newValue;
        firePropertyChange(P_HttpPort, old, this.httpPort);
    }
      
    @OAProperty(displayName = "Https Port", displayLength = 6, uiColumnLength = 10, format = "###")
    public int getHttpsPort() {
        return httpsPort;
    }
    public void setHttpsPort(int newValue) {
        int old = httpsPort;
        fireBeforePropertyChange(P_HttpsPort, old, newValue);
        this.httpsPort = newValue;
        firePropertyChange(P_HttpsPort, old, this.httpsPort);
    }
      
    @OAProperty(displayLength = 6)
    public int getId() {
        return id;
    }
    public void setId(int newValue) {
        int old = id;
        fireBeforePropertyChange(P_Id, old, newValue);
        this.id = newValue;
        firePropertyChange(P_Id, old, this.id);
    }
      
    @OAProperty(defaultValue = "new OADateTime()", displayLength = 15)
    public OADateTime getCreated() {
        return created;
    }
    public void setCreated(OADateTime newValue) {
        OADateTime old = created;
        fireBeforePropertyChange(P_Created, old, newValue);
        this.created = newValue;
        firePropertyChange(P_Created, old, this.created);
        if (isLoading()) return;
        if (created != null) {
            if (created2 == null) setCreated2(this.created.addDays(1));
            else if (created.compareTo(created2) > 0) setCreated2(this.created.addDays(1));
        }
    } 
    public OADateTime getCreated2() {
        return created2;
    }
    public void setCreated2(OADateTime newValue) {
        OADateTime old = created2;
        fireBeforePropertyChange(P_Created2, old, newValue);
        this.created2 = newValue;
        firePropertyChange(P_Created2, old, this.created2);
        if (created != null && created2 != null) {
            if (created.compareTo(created2) > 0) setCreated(this.created2);
        }
    }

    public int getMaxResults() {
        return maxResults;
    }
    public void setMaxResults(int newValue) {
        fireBeforePropertyChange(P_MaxResults, this.maxResults, newValue);
        int old = maxResults;
        this.maxResults = newValue;
        firePropertyChange(P_MaxResults, old, this.maxResults);
    }

    @OAOne
    public ApplicationType getApplicationType() {
        if (applicationType == null) {
            applicationType = (ApplicationType) getObject(P_ApplicationType);
        }
        return applicationType;
    }
    public void setApplicationType(ApplicationType newValue) {
        ApplicationType old = this.applicationType;
        this.applicationType = newValue;
        firePropertyChange(P_ApplicationType, old, this.applicationType);
    }
    public boolean getUseApplicationTypeSearch() {
        return useApplicationTypeSearch;
    }
    public void setUseApplicationTypeSearch(boolean newValue) {
        boolean old = this.useApplicationTypeSearch;
        this.useApplicationTypeSearch = newValue;
        firePropertyChange(P_UseApplicationTypeSearch, old, this.useApplicationTypeSearch);
    }
    public ApplicationTypeSearch getApplicationTypeSearch() {
        return this.searchApplicationType;
    }
    public void setApplicationTypeSearch(ApplicationTypeSearch newValue) {
        this.searchApplicationType = newValue;
    }

    public void reset() {
        setClientPort(0);
        setNull(P_ClientPort);
        setHttpPort(0);
        setNull(P_HttpPort);
        setHttpsPort(0);
        setNull(P_HttpsPort);
        setId(0);
        setNull(P_Id);
        setCreated(null);
        setCreated2(null);
        setApplicationType(null);
        setUseApplicationTypeSearch(false);
    }

    public boolean isDataEntered() {
        if (!isNull(P_ClientPort)) return true;
        if (!isNull(P_HttpPort)) return true;
        if (!isNull(P_HttpsPort)) return true;
        if (!isNull(P_Id)) return true;
        if (getCreated() != null) return true;
        if (getApplicationType() != null) return true;
        if (getUseApplicationTypeSearch()) return true;
        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<ServerApplication> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (OAString.isNotEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<ServerApplication> f = new OAQueryFilter<ServerApplication>(ServerApplication.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<ServerApplication> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<ServerApplication> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<ServerApplication> getSelect() {
        final String prefix = "";
        String sql = "";
        String sortOrder = null;
        Object[] args = new Object[0];
        OAFinder finder = null;
        if (!isNull(P_ClientPort)) {
            if (sql.length() > 0) sql += " AND ";
            sql += ServerApplication.P_ClientPort + " = ?";
            args = OAArray.add(Object.class, args, this.clientPort);
        }
        if (!isNull(P_HttpPort)) {
            if (sql.length() > 0) sql += " AND ";
            sql += ServerApplication.P_HttpPort + " = ?";
            args = OAArray.add(Object.class, args, this.httpPort);
        }
        if (!isNull(P_HttpsPort)) {
            if (sql.length() > 0) sql += " AND ";
            sql += ServerApplication.P_HttpsPort + " = ?";
            args = OAArray.add(Object.class, args, this.httpsPort);
        }
        if (!isNull(P_Id)) {
            if (sql.length() > 0) sql += " AND ";
            sql += ServerApplication.P_Id + " = ?";
            args = OAArray.add(Object.class, args, this.id);
        }
        if (created != null) {
            if (sql.length() > 0) sql += " AND ";
            if (created2 != null && !created.equals(created2)) {
                sql += ServerApplication.P_Created + " >= ?";
                args = OAArray.add(Object.class, args, this.created);
                sql += " AND " + ServerApplication.P_Created + " <= ?";
                args = OAArray.add(Object.class, args, this.created2);
            }
            else {
                sql += ServerApplication.P_Created + " = ?";
                args = OAArray.add(Object.class, args, this.created);
            }
        }
        if (!useApplicationTypeSearch && getApplicationType() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += ServerApplicationPP.applicationType().pp + " = ?";
            args = OAArray.add(Object.class, args, getApplicationType());
            finder = new OAFinder<ApplicationType, ServerApplication>(getApplicationType(), ApplicationType.P_ServerApplications);
        }

        if (OAString.isNotEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<ServerApplication> select = new OASelect<ServerApplication>(ServerApplication.class, sql, args, sortOrder);
        if (getExtraWhereFilter() != null && getExtraWhereFilter().updateSelect(select)) {
            select.setFilter(new OAAndFilter(this.getCustomFilter(), getExtraWhereFilter()));
        }
        else select.setFilter(this.getCustomFilter());
        select.setDataSourceFilter(this.getDataSourceFilter());
        select.setFinder(finder);
        if (getMaxResults() > 0) select.setMax(getMaxResults());
        if (useApplicationTypeSearch && getApplicationTypeSearch() != null) {
            getApplicationTypeSearch().appendSelect(ServerApplicationPP.applicationType().pp, select);
        }
        return select;
    }

    public void appendSelect(final String fromName, final OASelect select) {
        final String prefix = fromName + ".";
        String sql = "";
        Object[] args = new Object[0];
        if (!isNull(P_ClientPort)) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ServerApplication.P_ClientPort + " = ?";
            args = OAArray.add(Object.class, args, this.clientPort);
        }
        if (!isNull(P_HttpPort)) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ServerApplication.P_HttpPort + " = ?";
            args = OAArray.add(Object.class, args, this.httpPort);
        }
        if (!isNull(P_HttpsPort)) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ServerApplication.P_HttpsPort + " = ?";
            args = OAArray.add(Object.class, args, this.httpsPort);
        }
        if (!isNull(P_Id)) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ServerApplication.P_Id + " = ?";
            args = OAArray.add(Object.class, args, this.id);
        }
        if (created != null) {
            if (sql.length() > 0) sql += " AND ";
            if (created2 != null && !created.equals(created2)) {
                sql += prefix + ServerApplication.P_Created + " >= ?";
                args = OAArray.add(Object.class, args, this.created);
                sql += " AND " + prefix + ServerApplication.P_Created + " <= ?";
                args = OAArray.add(Object.class, args, this.created2);
            }
            else {
                sql += prefix + ServerApplication.P_Created + " = ?";
                args = OAArray.add(Object.class, args, this.created);
            }
        }
        if (!useApplicationTypeSearch && getApplicationType() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ServerApplicationPP.applicationType().pp + " = ?";
            args = OAArray.add(Object.class, args, getApplicationType());
        }
        if (useApplicationTypeSearch && getApplicationTypeSearch() != null) {
            getApplicationTypeSearch().appendSelect(prefix + ServerApplicationPP.applicationType().pp, select);
        }
        select.add(sql, args);
    }

    private OAFilter<ServerApplication> filterDataSourceFilter;
    public OAFilter<ServerApplication> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<ServerApplication>() {
            @Override
            public boolean isUsed(ServerApplication serverApplication) {
                return ServerApplicationSearch.this.isUsedForDataSourceFilter(serverApplication);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<ServerApplication> filterCustomFilter;
    public OAFilter<ServerApplication> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<ServerApplication>() {
            @Override
            public boolean isUsed(ServerApplication serverApplication) {
                boolean b = ServerApplicationSearch.this.isUsedForCustomFilter(serverApplication);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(ServerApplication searchServerApplication) {
        return true;
    }
    public boolean isUsedForCustomFilter(ServerApplication searchServerApplication) {
        return true;
    }
}
