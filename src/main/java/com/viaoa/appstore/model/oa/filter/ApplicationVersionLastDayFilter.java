// Generated by OABuilder
package com.viaoa.appstore.model.oa.filter;

import java.util.*;
import java.util.logging.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import java.util.*;
import com.viaoa.appstore.model.search.*;
import com.viaoa.appstore.model.oa.search.*;

@OAClass(useDataSource=false, localOnly=true)
@OAClassFilter(name = "LastDay", displayName = "Last Day", hasInputParams = false)
public class ApplicationVersionLastDayFilter extends OAObject implements CustomHubFilter<ApplicationVersion> {
    private static final long serialVersionUID = 1L;

    private static Logger LOG = Logger.getLogger(ApplicationVersionLastDayFilter.class.getName());


    public static final String PPCode = ":LastDay()";
    private Hub<ApplicationVersion> hubMaster;
    private Hub<ApplicationVersion> hub;
    private HubFilter<ApplicationVersion> hubFilter;
    private OAObjectCacheFilter<ApplicationVersion> cacheFilter;
    private boolean bUseObjectCache;

    public ApplicationVersionLastDayFilter() {
        this(null, null, false);
    }
    public ApplicationVersionLastDayFilter(Hub<ApplicationVersion> hub) {
        this(null, hub, true);
    }
    public ApplicationVersionLastDayFilter(Hub<ApplicationVersion> hubMaster, Hub<ApplicationVersion> hub) {
        this(hubMaster, hub, false);
    }
    public ApplicationVersionLastDayFilter(Hub<ApplicationVersion> hubMaster, Hub<ApplicationVersion> hubFiltered, boolean bUseObjectCache) {
        this.hubMaster = hubMaster;
        this.hub = hubFiltered;
        this.bUseObjectCache = bUseObjectCache;
        if (hubMaster != null) getHubFilter();
        if (bUseObjectCache) getObjectCacheFilter();
    }



    public void reset() {
    }

    public boolean isDataEntered() {
        return false;
    }
    public void refresh() {
        if (hubFilter != null) getHubFilter().refresh();
        if (cacheFilter != null) getObjectCacheFilter().refresh();
    }

    @Override
    public HubFilter<ApplicationVersion> getHubFilter() {
        if (hubFilter != null) return hubFilter;
        if (hubMaster == null) return null;
        hubFilter = new HubFilter<ApplicationVersion>(hubMaster, hub) {
            @Override
            public boolean isUsed(ApplicationVersion applicationVersion) {
                return ApplicationVersionLastDayFilter.this.isUsed(applicationVersion);
            }
        };
        hubFilter.addDependentProperty(ApplicationVersionPP.started(), false);
        hubFilter.refresh();
        return hubFilter;
    }

    public OAObjectCacheFilter<ApplicationVersion> getObjectCacheFilter() {
        if (cacheFilter != null) return cacheFilter;
        if (!bUseObjectCache) return null;
        hub.onBeforeRefresh(e -> reselect());
        cacheFilter = new OAObjectCacheFilter<ApplicationVersion>(hub) {
            @Override
            public boolean isUsed(ApplicationVersion applicationVersion) {
                return ApplicationVersionLastDayFilter.this.isUsed(applicationVersion);
            }
            @Override
            protected void reselect() {
                ApplicationVersionLastDayFilter.this.reselect();
            }
        };
        cacheFilter.addDependentProperty(ApplicationVersionPP.started(), false);
        cacheFilter.refresh();
        return cacheFilter;
    }

    public void reselect() {
        // can be overwritten to query datasource
    }

    // ==================
    // this method has custom code that will need to be put into the OABuilder filter

    @Override
    public boolean isUsed(ApplicationVersion applicationVersion) {
        OADateTime started = applicationVersion.getStarted();
        if (started == null) return false;
        if (started.before(new OADateTime(new OADate()))) return false;
        return true;
    }
}
