// Generated by OABuilder
package com.viaoa.appstore.model.oa.search;

import java.util.*;
import java.util.logging.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.OADateTime;
import com.viaoa.datasource.*;
import com.viaoa.filter.*;

@OAClass(useDataSource=false, localOnly=true)
public class ApplicationVersionSearch extends OAObject {
    private static final long serialVersionUID = 1L;

    private static Logger LOG = Logger.getLogger(ApplicationVersionSearch.class.getName());

    public static final String P_Created = "Created";
    public static final String P_Version = "Version";
    public static final String P_Invalid = "Invalid";
    public static final String P_InvalidUseNull = "InvalidUseNull";
    public static final String P_InvalidUseNotNull = "InvalidUseNotNull";
    public static final String P_ApplicationType = "ApplicationType";
    public static final String P_UseApplicationTypeSearch = "UseApplicationTypeSearch";
    public static final String P_MaxResults = "MaxResults";

    protected OADateTime created;
    protected String version;
    protected boolean invalid;
    protected boolean invalidUseNull;
    protected boolean invalidUseNotNull;
    protected ApplicationType applicationType;
    protected boolean useApplicationTypeSearch;
    protected ApplicationTypeSearch searchApplicationType;
    protected int maxResults;

    @OAProperty(defaultValue = "new OADateTime()", displayLength = 15)
    public OADateTime getCreated() {
        return created;
    }
    public void setCreated(OADateTime newValue) {
        OADateTime old = created;
        fireBeforePropertyChange(P_Created, old, newValue);
        this.created = newValue;
        firePropertyChange(P_Created, old, this.created);
    }
      
    @OAProperty(maxLength = 12, displayLength = 6)
    public String getVersion() {
        return version;
    }
    public void setVersion(String newValue) {
        String old = version;
        fireBeforePropertyChange(P_Version, old, newValue);
        this.version = newValue;
        firePropertyChange(P_Version, old, this.version);
    }
      
    @OAProperty(displayLength = 5, uiColumnLength = 7)
    public boolean getInvalid() {
        return invalid;
    }
    public boolean isInvalid() {
        return getInvalid();
    }
    public void setInvalid(boolean newValue) {
        boolean old = invalid;
        fireBeforePropertyChange(P_Invalid, old, newValue);
        this.invalid = newValue;
        firePropertyChange(P_Invalid, old, this.invalid);
    }
      
    public boolean getInvalidUseNull() {
        return invalidUseNull;
    }
    public void setInvalidUseNull(boolean newValue) {
        boolean old = this.invalidUseNull;
        this.invalidUseNull = newValue;
        firePropertyChange(P_InvalidUseNull, old, this.invalidUseNull);
    }
    public boolean getInvalidUseNotNull() {
        return invalidUseNotNull;
    }
    public void setInvalidUseNotNull(boolean newValue) {
        boolean old = this.invalidUseNotNull;
        this.invalidUseNotNull = newValue;
        firePropertyChange(P_InvalidUseNotNull, old, this.invalidUseNotNull);
    }

    public int getMaxResults() {
        return maxResults;
    }
    public void setMaxResults(int newValue) {
        fireBeforePropertyChange(P_MaxResults, this.maxResults, newValue);
        int old = maxResults;
        this.maxResults = newValue;
        firePropertyChange(P_MaxResults, old, this.maxResults);
    }

    @OAOne
    public ApplicationType getApplicationType() {
        if (applicationType == null) {
            applicationType = (ApplicationType) getObject(P_ApplicationType);
        }
        return applicationType;
    }
    public void setApplicationType(ApplicationType newValue) {
        ApplicationType old = this.applicationType;
        this.applicationType = newValue;
        firePropertyChange(P_ApplicationType, old, this.applicationType);
    }
    public boolean getUseApplicationTypeSearch() {
        return useApplicationTypeSearch;
    }
    public void setUseApplicationTypeSearch(boolean newValue) {
        boolean old = this.useApplicationTypeSearch;
        this.useApplicationTypeSearch = newValue;
        firePropertyChange(P_UseApplicationTypeSearch, old, this.useApplicationTypeSearch);
    }
    public ApplicationTypeSearch getApplicationTypeSearch() {
        return this.searchApplicationType;
    }
    public void setApplicationTypeSearch(ApplicationTypeSearch newValue) {
        this.searchApplicationType = newValue;
    }

    public void reset() {
        setCreated(null);
        setVersion(null);
        setInvalid(false);
        setNull(P_Invalid);
        setInvalidUseNull(false);
        setInvalidUseNotNull(false);
        setApplicationType(null);
        setUseApplicationTypeSearch(false);
    }

    public boolean isDataEntered() {
        if (getCreated() != null) return true;
        if (getVersion() != null) return true;
        if (!isNull(P_Invalid)) return true;
        if (getInvalidUseNull()) return true;
        if (getInvalidUseNotNull()) return true;
        if (getApplicationType() != null) return true;
        if (getUseApplicationTypeSearch()) return true;
        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<ApplicationVersion> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (OAString.isNotEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<ApplicationVersion> f = new OAQueryFilter<ApplicationVersion>(ApplicationVersion.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<ApplicationVersion> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<ApplicationVersion> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<ApplicationVersion> getSelect() {
        final String prefix = "";
        String sql = "";
        String sortOrder = null;
        Object[] args = new Object[0];
        OAFinder finder = null;
        if (created != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += ApplicationVersion.P_Created + " = ?";
            args = OAArray.add(Object.class, args, this.created);
        }
        if (OAString.isNotEmpty(this.version)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(version);
            if (val.indexOf("%") >= 0) {
                sql += ApplicationVersion.P_Version + " LIKE ?";
            }
            else {
                sql += ApplicationVersion.P_Version + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (invalidUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += ApplicationVersion.P_Invalid + " = null";
        }
        else if (invalidUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += ApplicationVersion.P_Invalid + " != null";
        }
        if (!isNull(P_Invalid)) {
            if (sql.length() > 0) sql += " AND ";
            sql += ApplicationVersion.P_Invalid + " = ?";
            args = OAArray.add(Object.class, args, this.invalid);
        }
        if (!useApplicationTypeSearch && getApplicationType() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += ApplicationVersionPP.applicationType().pp + " = ?";
            args = OAArray.add(Object.class, args, getApplicationType());
            finder = new OAFinder<ApplicationType, ApplicationVersion>(getApplicationType(), ApplicationType.P_ApplicationVersions);
        }

        if (OAString.isNotEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<ApplicationVersion> select = new OASelect<ApplicationVersion>(ApplicationVersion.class, sql, args, sortOrder);
        if (getExtraWhereFilter() != null && getExtraWhereFilter().updateSelect(select)) {
            select.setFilter(new OAAndFilter(this.getCustomFilter(), getExtraWhereFilter()));
        }
        else select.setFilter(this.getCustomFilter());
        select.setDataSourceFilter(this.getDataSourceFilter());
        select.setFinder(finder);
        if (getMaxResults() > 0) select.setMax(getMaxResults());
        if (useApplicationTypeSearch && getApplicationTypeSearch() != null) {
            getApplicationTypeSearch().appendSelect(ApplicationVersionPP.applicationType().pp, select);
        }
        return select;
    }

    public void appendSelect(final String fromName, final OASelect select) {
        final String prefix = fromName + ".";
        String sql = "";
        Object[] args = new Object[0];
        if (created != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ApplicationVersion.P_Created + " = ?";
            args = OAArray.add(Object.class, args, this.created);
        }
        if (OAString.isNotEmpty(this.version)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(version);
            if (val.indexOf("%") >= 0) {
                sql += prefix + ApplicationVersion.P_Version + " LIKE ?";
            }
            else {
                sql += prefix + ApplicationVersion.P_Version + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (invalidUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ApplicationVersion.P_Invalid + " = null";
        }
        else if (invalidUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ApplicationVersion.P_Invalid + " != null";
        }
        if (!isNull(P_Invalid)) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ApplicationVersion.P_Invalid + " = ?";
            args = OAArray.add(Object.class, args, this.invalid);
        }
        if (!useApplicationTypeSearch && getApplicationType() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ApplicationVersionPP.applicationType().pp + " = ?";
            args = OAArray.add(Object.class, args, getApplicationType());
        }
        if (useApplicationTypeSearch && getApplicationTypeSearch() != null) {
            getApplicationTypeSearch().appendSelect(prefix + ApplicationVersionPP.applicationType().pp, select);
        }
        select.add(sql, args);
    }

    private OAFilter<ApplicationVersion> filterDataSourceFilter;
    public OAFilter<ApplicationVersion> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<ApplicationVersion>() {
            @Override
            public boolean isUsed(ApplicationVersion applicationVersion) {
                return ApplicationVersionSearch.this.isUsedForDataSourceFilter(applicationVersion);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<ApplicationVersion> filterCustomFilter;
    public OAFilter<ApplicationVersion> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<ApplicationVersion>() {
            @Override
            public boolean isUsed(ApplicationVersion applicationVersion) {
                boolean b = ApplicationVersionSearch.this.isUsedForCustomFilter(applicationVersion);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(ApplicationVersion searchApplicationVersion) {
        return true;
    }
    public boolean isUsedForCustomFilter(ApplicationVersion searchApplicationVersion) {
        return true;
    }
}
