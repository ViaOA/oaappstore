// Generated by OABuilder
package com.viaoa.appstore.model.oa.search;

import java.util.*;
import java.util.logging.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.datasource.*;
import com.viaoa.filter.*;

@OAClass(useDataSource=false, localOnly=true)
public class EnvironmentSearch extends OAObject {
    private static final long serialVersionUID = 1L;

    private static Logger LOG = Logger.getLogger(EnvironmentSearch.class.getName());

    public static final String P_Name = "Name";
    public static final String P_AbbrevName = "AbbrevName";
    public static final String P_Type = "Type";
    public static final String P_Type2 = "Type2";
    public static final String P_Id = "Id";
    public static final String P_Id2 = "Id2";
    public static final String P_MaxResults = "MaxResults";

    protected String name;
    protected String abbrevName;
    protected int type;
    protected int type2;
    protected int id;
    protected int id2;
    protected int maxResults;

    @OAProperty(maxLength = 35, displayLength = 15)
    public String getName() {
        return name;
    }
    public void setName(String newValue) {
        String old = name;
        fireBeforePropertyChange(P_Name, old, newValue);
        this.name = newValue;
        firePropertyChange(P_Name, old, this.name);
    }
      
    @OAProperty(displayName = "Abbrev Name", maxLength = 8, displayLength = 5, uiColumnLength = 6, uiColumnName = "Abbrev")
    public String getAbbrevName() {
        return abbrevName;
    }
    public void setAbbrevName(String newValue) {
        String old = abbrevName;
        fireBeforePropertyChange(P_AbbrevName, old, newValue);
        this.abbrevName = newValue;
        firePropertyChange(P_AbbrevName, old, this.abbrevName);
    }
      
    @OAProperty(displayLength = 16, isNameValue = true)
    public int getType() {
        return type;
    }
    public void setType(int newValue) {
        int old = type;
        fireBeforePropertyChange(P_Type, old, newValue);
        this.type = newValue;
        firePropertyChange(P_Type, old, this.type);
        if (isLoading()) return;
        if (type > type2) setType2(this.type);
    } 
    public int getType2() {
        return type2;
    }
    public void setType2(int newValue) {
        int old = type2;
        fireBeforePropertyChange(P_Type2, old, newValue);
        this.type2 = newValue;
        firePropertyChange(P_Type2, old, this.type2);
        if (isLoading()) return;
        if (type > type2) setType(this.type2);
    }

    public String getTypeString() {
        Environment.Type type = getTypeEnum();
        if (type == null) return null;
        return type.name();
    }
    public void setTypeString(String val) {
        int x = -1;
        if (OAString.isNotEmpty(val)) {
            Environment.Type type = Environment.Type.valueOf(val);
            if (type != null) x = type.ordinal();
        }
        if (x < 0) setNull(P_Type);
        else setType(x);
    }

    public Environment.Type getTypeEnum() {
        if (isNull(P_Type)) return null;
        final int val = getType();
        if (val < 0 || val >= Environment.Type.values().length) return null;
        return Environment.Type.values()[val];
    }

    public void setTypeEnum(Environment.Type val) {
        if (val == null) {
            setNull(P_Type);
        }
        else {
            setType(val.ordinal());
        }
    }
    @OAProperty(displayLength = 6)
    public int getId() {
        return id;
    }
    public void setId(int newValue) {
        int old = id;
        fireBeforePropertyChange(P_Id, old, newValue);
        this.id = newValue;
        firePropertyChange(P_Id, old, this.id);
        if (isLoading()) return;
        if (id > id2) setId2(this.id);
    } 
    public int getId2() {
        return id2;
    }
    public void setId2(int newValue) {
        int old = id2;
        fireBeforePropertyChange(P_Id2, old, newValue);
        this.id2 = newValue;
        firePropertyChange(P_Id2, old, this.id2);
        if (isLoading()) return;
        if (id > id2) setId(this.id2);
    }

    public int getMaxResults() {
        return maxResults;
    }
    public void setMaxResults(int newValue) {
        fireBeforePropertyChange(P_MaxResults, this.maxResults, newValue);
        int old = maxResults;
        this.maxResults = newValue;
        firePropertyChange(P_MaxResults, old, this.maxResults);
    }

    public void reset() {
        setName(null);
        setAbbrevName(null);
        setType(0);
        setNull(P_Type);
        setType2(0);
        setNull(P_Type2);
        setId(0);
        setNull(P_Id);
        setId2(0);
        setNull(P_Id2);
    }

    public boolean isDataEntered() {
        if (getName() != null) return true;
        if (getAbbrevName() != null) return true;
        if (!isNull(P_Type)) return true;
        if (!isNull(P_Id)) return true;
        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<Environment> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (OAString.isNotEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<Environment> f = new OAQueryFilter<Environment>(Environment.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<Environment> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<Environment> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<Environment> getSelect() {
        final String prefix = "";
        String sql = "";
        String sortOrder = null;
        Object[] args = new Object[0];
        if (OAString.isNotEmpty(this.name)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(name);
            if (val.indexOf("%") >= 0) {
                sql += Environment.P_Name + " LIKE ?";
            }
            else {
                sql += Environment.P_Name + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (OAString.isNotEmpty(this.abbrevName)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(abbrevName);
            if (val.indexOf("%") >= 0) {
                sql += Environment.P_AbbrevName + " LIKE ?";
            }
            else {
                sql += Environment.P_AbbrevName + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (!isNull(P_Type)) {
            if (sql.length() > 0) sql += " AND ";
            if (!isNull(P_Type2) && type != type2) {
                sql += Environment.P_Type + " >= ?";
                args = OAArray.add(Object.class, args, getType());
                sql += " AND " + Environment.P_Type + " <= ?";
                args = OAArray.add(Object.class, args, getType2());
            }
            else {
                sql += Environment.P_Type + " = ?";
                args = OAArray.add(Object.class, args, getType());
            }
        }
        if (!isNull(P_Id)) {
            if (sql.length() > 0) sql += " AND ";
            if (!isNull(P_Id2) && id != id2) {
                sql += Environment.P_Id + " >= ?";
                args = OAArray.add(Object.class, args, getId());
                sql += " AND " + Environment.P_Id + " <= ?";
                args = OAArray.add(Object.class, args, getId2());
            }
            else {
                sql += Environment.P_Id + " = ?";
                args = OAArray.add(Object.class, args, getId());
            }
        }

        if (OAString.isNotEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<Environment> select = new OASelect<Environment>(Environment.class, sql, args, sortOrder);
        if (getExtraWhereFilter() != null && getExtraWhereFilter().updateSelect(select)) {
            select.setFilter(new OAAndFilter(this.getCustomFilter(), getExtraWhereFilter()));
        }
        else select.setFilter(this.getCustomFilter());
        select.setDataSourceFilter(this.getDataSourceFilter());
        if (getMaxResults() > 0) select.setMax(getMaxResults());
        return select;
    }

    public void appendSelect(final String fromName, final OASelect select) {
        final String prefix = fromName + ".";
        String sql = "";
        Object[] args = new Object[0];
        if (OAString.isNotEmpty(this.name)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(name);
            if (val.indexOf("%") >= 0) {
                sql += prefix + Environment.P_Name + " LIKE ?";
            }
            else {
                sql += prefix + Environment.P_Name + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (OAString.isNotEmpty(this.abbrevName)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(abbrevName);
            if (val.indexOf("%") >= 0) {
                sql += prefix + Environment.P_AbbrevName + " LIKE ?";
            }
            else {
                sql += prefix + Environment.P_AbbrevName + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (!isNull(P_Type)) {
            if (sql.length() > 0) sql += " AND ";
            if (!isNull(P_Type2) && type != type2) {
                sql += prefix + Environment.P_Type + " >= ?";
                args = OAArray.add(Object.class, args, getType());
                sql += " AND " + prefix + Environment.P_Type + " <= ?";
                args = OAArray.add(Object.class, args, getType2());
            }
            else {
                sql += prefix + Environment.P_Type + " = ?";
                args = OAArray.add(Object.class, args, getType());
            }
        }
        if (!isNull(P_Id)) {
            if (sql.length() > 0) sql += " AND ";
            if (!isNull(P_Id2) && id != id2) {
                sql += prefix + Environment.P_Id + " >= ?";
                args = OAArray.add(Object.class, args, getId());
                sql += " AND " + prefix + Environment.P_Id + " <= ?";
                args = OAArray.add(Object.class, args, getId2());
            }
            else {
                sql += prefix + Environment.P_Id + " = ?";
                args = OAArray.add(Object.class, args, getId());
            }
        }
        select.add(sql, args);
    }

    private OAFilter<Environment> filterDataSourceFilter;
    public OAFilter<Environment> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<Environment>() {
            @Override
            public boolean isUsed(Environment environment) {
                return EnvironmentSearch.this.isUsedForDataSourceFilter(environment);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<Environment> filterCustomFilter;
    public OAFilter<Environment> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<Environment>() {
            @Override
            public boolean isUsed(Environment environment) {
                boolean b = EnvironmentSearch.this.isUsedForCustomFilter(environment);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(Environment searchEnvironment) {
        return true;
    }
    public boolean isUsedForCustomFilter(Environment searchEnvironment) {
        return true;
    }
}
