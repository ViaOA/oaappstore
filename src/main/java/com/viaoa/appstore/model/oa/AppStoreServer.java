// Generated by OABuilder
package com.viaoa.appstore.model.oa;
 
import java.util.*;
import java.util.logging.*;
import java.sql.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.scheduler.*;
import com.viaoa.util.*;
import com.viaoa.annotation.*;
import com.viaoa.util.OADateTime;
import com.viaoa.appstore.delegate.oa.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.model.oa.propertypath.*;
 
@OAClass(
    lowerName = "appStoreServer",
    pluralName = "AppStoreServers",
    shortName = "ass",
    displayName = "App Store Server",
    isLookup = true,
    isPreSelect = true,
    displayProperty = "name",
    noPojo = true
)
@OATable(
    indexes = {
        @OAIndex(name = "AppStoreServerRunningApp", fkey = true, columns = { @OAIndexColumn(name = "RunningAppId") })
    }
)
public class AppStoreServer extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(AppStoreServer.class.getName());

    public static final String P_Id = "id";
    public static final String P_Created = "created";
    public static final String P_Name = "name";
    public static final String P_Server = "server";
    public static final String P_Console = "console";
     
    public static final String P_RunningApp = "runningApp";
    public static final String P_RunningAppId = "runningAppId"; // fkey
     
    public static final String M_Run = "run";
    protected volatile int id;
    protected volatile OADateTime created;
    protected volatile String name;
    protected volatile String server;
    protected volatile String console;
     
    // Links to other objects.
    protected volatile transient RunningApp runningApp;
     
    public AppStoreServer() {
        if (!isLoading()) setObjectDefaults();
    }
    @Override
    public void setObjectDefaults() {
        setCreated(new OADateTime());
    }
     
    public AppStoreServer(int id) {
        this();
        setId(id);
    }

    @OAProperty(isUnique = true, trackPrimitiveNull = false, displayLength = 6)
    @OAId
    @OAColumn(name = "Id", sqlType = java.sql.Types.INTEGER)
    public int getId() {
        return id;
    }
    public void setId(int newValue) {
        int old = id;
        fireBeforePropertyChange(P_Id, old, newValue);
        this.id = newValue;
        firePropertyChange(P_Id, old, this.id);
    }

    @OAProperty(defaultValue = "new OADateTime()", displayLength = 15, isProcessed = true, ignoreTimeZone = true)
    @OAColumn(name = "Created", sqlType = java.sql.Types.TIMESTAMP)
    public OADateTime getCreated() {
        return created;
    }
    public void setCreated(OADateTime newValue) {
        OADateTime old = created;
        fireBeforePropertyChange(P_Created, old, newValue);
        this.created = newValue;
        firePropertyChange(P_Created, old, this.created);
    }

    @OAProperty(maxLength = 70, displayLength = 20)
    @OAColumn(name = "Name", maxLength = 70)
    public String getName() {
        return name;
    }
    public void setName(String newValue) {
        String old = name;
        fireBeforePropertyChange(P_Name, old, newValue);
        this.name = newValue;
        firePropertyChange(P_Name, old, this.name);
    }

    @OAProperty(maxLength = 75, displayLength = 20)
    @OAColumn(name = "Server", maxLength = 75)
    public String getServer() {
        return server;
    }
    public void setServer(String newValue) {
        String old = server;
        fireBeforePropertyChange(P_Server, old, newValue);
        this.server = newValue;
        firePropertyChange(P_Server, old, this.server);
    }

    @OAProperty(maxLength = 254, displayLength = 22, uiColumnLength = 20)
    public String getConsole() {
        return console;
    }
    public void setConsole(String newValue) {
        String old = console;
        fireBeforePropertyChange(P_Console, old, newValue);
        this.console = newValue;
        firePropertyChange(P_Console, old, this.console);
    }

    @OAOne(
        displayName = "Running App", 
        reverseName = RunningApp.P_AppStoreServer, 
        allowCreateNew = false, 
        allowAddExisting = false, 
        fkeys = {@OAFkey(fromProperty = P_RunningAppId, toProperty = RunningApp.P_Id)}
    )
    public RunningApp getRunningApp() {
        if (runningApp == null) {
            runningApp = (RunningApp) getObject(P_RunningApp);
        }
        return runningApp;
    }
    public void setRunningApp(RunningApp newValue) {
        RunningApp old = this.runningApp;
        fireBeforePropertyChange(P_RunningApp, old, newValue);
        this.runningApp = newValue;
        firePropertyChange(P_RunningApp, old, this.runningApp);
    }
    @OAProperty(isFkeyOnly = true)
    @OAColumn(name = "RunningAppId")
    public Integer getRunningAppId() {
        return (Integer) getFkeyProperty(P_RunningAppId);
    }
    public void setRunningAppId(Integer newValue) {
        this.runningApp = null;
        setFkeyProperty(P_RunningAppId, newValue);
    }
    @OAMethod(displayName = "Run")
    public void run() throws Exception {
        // custom code
        AppStoreServerDelegate.run(this);
    }

    public void load(ResultSet rs, int id) throws SQLException {
        this.id = id;
        java.sql.Timestamp timestamp;
        timestamp = rs.getTimestamp(2);
        if (timestamp != null) this.created = new OADateTime(timestamp);
        this.name = rs.getString(3);
        this.server = rs.getString(4);
        int runningAppFkey = rs.getInt(5);
        setFkeyProperty(P_RunningApp, rs.wasNull() ? null : runningAppFkey);

        this.changedFlag = false;
        this.newFlag = false;
    }
}
 
