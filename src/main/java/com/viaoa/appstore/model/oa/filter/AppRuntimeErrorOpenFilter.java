// Generated by OABuilder
package com.viaoa.appstore.model.oa.filter;

import java.util.*;
import java.util.logging.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import java.util.*;
import com.viaoa.appstore.model.search.*;
import com.viaoa.appstore.model.oa.search.*;

@OAClass(useDataSource=false, localOnly=true)
@OAClassFilter(name = "Open", displayName = "Open", hasInputParams = false, query = "ack == false")
public class AppRuntimeErrorOpenFilter extends OAObject implements CustomHubFilter<AppRuntimeError> {
    private static final long serialVersionUID = 1L;

    private static Logger LOG = Logger.getLogger(AppRuntimeErrorOpenFilter.class.getName());


    public static final String PPCode = ":Open()";
    private Hub<AppRuntimeError> hubMaster;
    private Hub<AppRuntimeError> hub;
    private HubFilter<AppRuntimeError> hubFilter;
    private OAObjectCacheFilter<AppRuntimeError> cacheFilter;
    private boolean bUseObjectCache;

    public AppRuntimeErrorOpenFilter() {
        this(null, null, false);
    }
    public AppRuntimeErrorOpenFilter(Hub<AppRuntimeError> hub) {
        this(null, hub, true);
    }
    public AppRuntimeErrorOpenFilter(Hub<AppRuntimeError> hubMaster, Hub<AppRuntimeError> hub) {
        this(hubMaster, hub, false);
    }
    public AppRuntimeErrorOpenFilter(Hub<AppRuntimeError> hubMaster, Hub<AppRuntimeError> hubFiltered, boolean bUseObjectCache) {
        this.hubMaster = hubMaster;
        this.hub = hubFiltered;
        this.bUseObjectCache = bUseObjectCache;
        if (hubMaster != null) getHubFilter();
        if (bUseObjectCache) getObjectCacheFilter();
    }



    public void reset() {
    }

    public boolean isDataEntered() {
        return false;
    }
    public void refresh() {
        if (hubFilter != null) getHubFilter().refresh();
        if (cacheFilter != null) getObjectCacheFilter().refresh();
    }

    @Override
    public HubFilter<AppRuntimeError> getHubFilter() {
        if (hubFilter != null) return hubFilter;
        if (hubMaster == null) return null;
        hubFilter = new HubFilter<AppRuntimeError>(hubMaster, hub) {
            @Override
            public boolean isUsed(AppRuntimeError appRuntimeError) {
                return AppRuntimeErrorOpenFilter.this.isUsed(appRuntimeError);
            }
        };
        hubFilter.addDependentProperty(AppRuntimeErrorPP.ack(), false);
        hubFilter.refresh();
        return hubFilter;
    }

    public OAObjectCacheFilter<AppRuntimeError> getObjectCacheFilter() {
        if (cacheFilter != null) return cacheFilter;
        if (!bUseObjectCache) return null;
        hub.onBeforeRefresh(e -> reselect());
        cacheFilter = new OAObjectCacheFilter<AppRuntimeError>(hub) {
            @Override
            public boolean isUsed(AppRuntimeError appRuntimeError) {
                return AppRuntimeErrorOpenFilter.this.isUsed(appRuntimeError);
            }
            @Override
            protected void reselect() {
                AppRuntimeErrorOpenFilter.this.reselect();
            }
        };
        cacheFilter.addDependentProperty(AppRuntimeErrorPP.ack(), false);
        cacheFilter.refresh();
        return cacheFilter;
    }

    public void reselect() {
        // can be overwritten to query datasource
    }

    // ==================
    // this method has custom code that will need to be put into the OABuilder filter

    @Override
    public boolean isUsed(AppRuntimeError error) {
        return !error.getAck();
    }
}
