// Generated by OABuilder
package com.viaoa.appstore.model.oa;
 
import java.util.*;
import java.util.logging.*;
import java.sql.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.scheduler.*;
import com.viaoa.util.*;
import com.viaoa.annotation.*;
import com.viaoa.util.OADateTime;
import com.viaoa.appstore.delegate.oa.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.model.oa.propertypath.*;
 
@OAClass(
    lowerName = "appRuntimeError",
    pluralName = "AppRuntimeErrors",
    shortName = "are",
    displayName = "App Runtime Error",
    isProcessed = true,
    displayProperty = "created",
    filterClasses = {AppRuntimeErrorOpenFilter.class},
    noPojo = true
)
@OATable(
    indexes = {
        @OAIndex(name = "AppRuntimeErrorAppRuntime", fkey = true, columns = { @OAIndexColumn(name = "AppRuntimeId") })
    }
)
public class AppRuntimeError extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(AppRuntimeError.class.getName());

    public static final String P_Id = "id";
    public static final String P_Created = "created";
    public static final String P_Message = "message";
    public static final String P_Ack = "ack";
    public static final String P_StackTrace = "stackTrace";
     
    public static final String P_AppRuntime = "appRuntime";
    public static final String P_AppRuntimeId = "appRuntimeId"; // fkey
     
    public static final String M_Acknowledge = "acknowledge";
    protected volatile int id;
    protected volatile OADateTime created;
    protected volatile String message;
    protected volatile boolean ack;
    protected volatile String stackTrace;
     
    // Links to other objects.
    protected volatile transient AppRuntime appRuntime;
     
    public AppRuntimeError() {
        if (!isLoading()) setObjectDefaults();
    }
    @Override
    public void setObjectDefaults() {
        setCreated(new OADateTime());
    }
     
    public AppRuntimeError(int id) {
        this();
        setId(id);
    }

    @OAProperty(isUnique = true, displayLength = 6)
    @OAId
    @OAColumn(name = "Id", sqlType = java.sql.Types.INTEGER)
    public int getId() {
        return id;
    }
    public void setId(int newValue) {
        int old = id;
        fireBeforePropertyChange(P_Id, old, newValue);
        this.id = newValue;
        firePropertyChange(P_Id, old, this.id);
    }

    @OAProperty(defaultValue = "new OADateTime()", displayLength = 15, isProcessed = true)
    @OAColumn(name = "Created", sqlType = java.sql.Types.TIMESTAMP)
    public OADateTime getCreated() {
        return created;
    }
    public void setCreated(OADateTime newValue) {
        OADateTime old = created;
        fireBeforePropertyChange(P_Created, old, newValue);
        this.created = newValue;
        firePropertyChange(P_Created, old, this.created);
    }

    @OAProperty(displayLength = 20)
    @OAColumn(name = "Message", maxLength = 0)
    public String getMessage() {
        return message;
    }
    public void setMessage(String newValue) {
        String old = message;
        fireBeforePropertyChange(P_Message, old, newValue);
        this.message = newValue;
        firePropertyChange(P_Message, old, this.message);
    }

    @OAProperty(trackPrimitiveNull = false, displayLength = 5)
    @OAColumn(name = "Acknowledged", sqlType = java.sql.Types.BOOLEAN)
    public boolean getAck() {
        return ack;
    }
    public boolean isAck() {
        return getAck();
    }
    public void setAck(boolean newValue) {
        boolean old = ack;
        fireBeforePropertyChange(P_Ack, old, newValue);
        this.ack = newValue;
        firePropertyChange(P_Ack, old, this.ack);
    }

    @OAProperty(displayName = "Stack Trace", displayLength = 30, uiColumnLength = 20, isProcessed = true)
    @OAColumn(name = "StackTrace", sqlType = java.sql.Types.CLOB)
    public String getStackTrace() {
        return stackTrace;
    }
    public void setStackTrace(String newValue) {
        String old = stackTrace;
        fireBeforePropertyChange(P_StackTrace, old, newValue);
        this.stackTrace = newValue;
        firePropertyChange(P_StackTrace, old, this.stackTrace);
    }

    @OAOne(
        displayName = "App Runtime", 
        reverseName = AppRuntime.P_AppRuntimeErrors, 
        required = true, 
        allowCreateNew = false, 
        fkeys = {@OAFkey(fromProperty = P_AppRuntimeId, toProperty = AppRuntime.P_Id)}
    )
    public AppRuntime getAppRuntime() {
        if (appRuntime == null) {
            appRuntime = (AppRuntime) getObject(P_AppRuntime);
        }
        return appRuntime;
    }
    public void setAppRuntime(AppRuntime newValue) {
        AppRuntime old = this.appRuntime;
        fireBeforePropertyChange(P_AppRuntime, old, newValue);
        this.appRuntime = newValue;
        firePropertyChange(P_AppRuntime, old, this.appRuntime);
    }
    @OAProperty(isFkeyOnly = true)
    @OAColumn(name = "AppRuntimeId")
    public Integer getAppRuntimeId() {
        return (Integer) getFkeyProperty(P_AppRuntimeId);
    }
    public void setAppRuntimeId(Integer newValue) {
        this.appRuntime = null;
        setFkeyProperty(P_AppRuntimeId, newValue);
    }
    @OAMethod(displayName = "Acknowledge")
    public void acknowledge() {
        // use this to run on server (remote)
        if (isRemoteAvailable()) {
            remote();
            return;
        }
        this.setAck(true);
    }
    public static void acknowledge(Hub<AppRuntimeError> hub) {
        if (isRemoteAvailable(hub)) {
            callRemote(hub);
            return;
        }
        for (AppRuntimeError appRuntimeError : hub) {
            appRuntimeError.acknowledge();
        }
    }

    public void load(ResultSet rs, int id) throws SQLException {
        this.id = id;
        java.sql.Timestamp timestamp;
        timestamp = rs.getTimestamp(2);
        if (timestamp != null) this.created = new OADateTime(timestamp);
        this.message = rs.getString(3);
        this.ack = rs.getBoolean(4);
        this.stackTrace = rs.getString(5);
        int appRuntimeFkey = rs.getInt(6);
        setFkeyProperty(P_AppRuntime, rs.wasNull() ? null : appRuntimeFkey);

        this.changedFlag = false;
        this.newFlag = false;
    }
}
 
