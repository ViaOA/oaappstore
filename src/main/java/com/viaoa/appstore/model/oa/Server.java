// Generated by OABuilder
package com.viaoa.appstore.model.oa;
 
import java.util.*;
import java.util.logging.*;
import java.sql.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.scheduler.*;
import com.viaoa.util.*;
import com.viaoa.annotation.*;
import com.viaoa.util.OADateTime;
import com.viaoa.appstore.delegate.oa.*;
import com.viaoa.appstore.model.oa.filter.*;
import com.viaoa.appstore.model.oa.propertypath.*;
 
@OAClass(
    lowerName = "server",
    pluralName = "Servers",
    shortName = "srv",
    displayName = "Server",
    displayProperty = "calcName",
    noPojo = true
)
@OATable(
    indexes = {
        @OAIndex(name = "ServerAppUser", fkey = true, columns = { @OAIndexColumn(name = "AppUserId") }), 
        @OAIndex(name = "ServerEnvironment", fkey = true, columns = { @OAIndexColumn(name = "EnvironmentId") })
    }
)
public class Server extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(Server.class.getName());

    public static final String P_Id = "id";
    public static final String P_Created = "created";
    public static final String P_Name = "name";
    public static final String P_Host = "host";
    public static final String P_IpAddress = "ipAddress";
     
    public static final String P_CalcName = "calcName";
     
    public static final String P_AppUser = "appUser";
    public static final String P_AppUserId = "appUserId"; // fkey
    public static final String P_Environment = "environment";
    public static final String P_EnvironmentId = "environmentId"; // fkey
    public static final String P_ServerApplications = "serverApplications";
     
    protected volatile int id;
    protected volatile OADateTime created;
    protected volatile String name;
    protected volatile String host;
    protected volatile String ipAddress;
     
    // Links to other objects.
    protected volatile transient AppUser appUser;
    protected volatile transient Environment environment;
    protected transient Hub<ServerApplication> hubServerApplications;
     
    public Server() {
        if (!isLoading()) setObjectDefaults();
    }
    @Override
    public void setObjectDefaults() {
        setCreated(new OADateTime());
    }
     
    public Server(int id) {
        this();
        setId(id);
    }
    @OAObjCallback(contextEnabledProperty = AppUser.P_Admin)
    public void callback(final OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        }
    }

    @OAProperty(isUnique = true, displayLength = 6)
    @OAId
    @OAColumn(name = "Id", sqlType = java.sql.Types.INTEGER)
    public int getId() {
        return id;
    }
    public void setId(int newValue) {
        int old = id;
        fireBeforePropertyChange(P_Id, old, newValue);
        this.id = newValue;
        firePropertyChange(P_Id, old, this.id);
    }

    @OAProperty(defaultValue = "new OADateTime()", displayLength = 15, isProcessed = true)
    @OAColumn(name = "Created", sqlType = java.sql.Types.TIMESTAMP)
    public OADateTime getCreated() {
        return created;
    }
    public void setCreated(OADateTime newValue) {
        OADateTime old = created;
        fireBeforePropertyChange(P_Created, old, newValue);
        this.created = newValue;
        firePropertyChange(P_Created, old, this.created);
    }

    @OAProperty(maxLength = 55, displayLength = 20, uiColumnLength = 18)
    @OAColumn(name = "Name", maxLength = 55)
    public String getName() {
        return name;
    }
    public void setName(String newValue) {
        String old = name;
        fireBeforePropertyChange(P_Name, old, newValue);
        this.name = newValue;
        firePropertyChange(P_Name, old, this.name);
    }

    @OAProperty(maxLength = 45, displayLength = 20)
    @OAColumn(name = "Host", maxLength = 45)
    public String getHost() {
        return host;
    }
    public void setHost(String newValue) {
        String old = host;
        fireBeforePropertyChange(P_Host, old, newValue);
        this.host = newValue;
        firePropertyChange(P_Host, old, this.host);
    }

    @OAProperty(displayName = "Ip Address", maxLength = 20, displayLength = 20)
    @OAColumn(name = "IpAddress", maxLength = 20)
    public String getIpAddress() {
        return ipAddress;
    }
    public void setIpAddress(String newValue) {
        String old = ipAddress;
        fireBeforePropertyChange(P_IpAddress, old, newValue);
        this.ipAddress = newValue;
        firePropertyChange(P_IpAddress, old, this.ipAddress);
    }
    @OACalculatedProperty(displayName = "Name", displayLength = 20, properties = {P_Name, P_Host, P_IpAddress})
    public String getCalcName() {
        String name = this.getName();
        if (OAStr.isNotEmpty(name)) return name;    
        String host = this.getHost();
        if (OAStr.isNotEmpty(host)) return host;
        return this.getIpAddress();
    }

    @OAOne(
        displayName = "App User", 
        reverseName = AppUser.P_Server, 
        allowCreateNew = false, 
        fkeys = {@OAFkey(fromProperty = P_AppUserId, toProperty = AppUser.P_Id)}
    )
    public AppUser getAppUser() {
        if (appUser == null) {
            appUser = (AppUser) getObject(P_AppUser);
        }
        return appUser;
    }
    public void setAppUser(AppUser newValue) {
        AppUser old = this.appUser;
        fireBeforePropertyChange(P_AppUser, old, newValue);
        this.appUser = newValue;
        firePropertyChange(P_AppUser, old, this.appUser);
    }
    @OAProperty(isFkeyOnly = true)
    @OAColumn(name = "AppUserId")
    public Integer getAppUserId() {
        return (Integer) getFkeyProperty(P_AppUserId);
    }
    public void setAppUserId(Integer newValue) {
        this.appUser = null;
        setFkeyProperty(P_AppUserId, newValue);
    }

    @OAOne(
        reverseName = Environment.P_Servers, 
        required = true, 
        allowCreateNew = false, 
        fkeys = {@OAFkey(fromProperty = P_EnvironmentId, toProperty = Environment.P_Id)}
    )
    public Environment getEnvironment() {
        if (environment == null) {
            environment = (Environment) getObject(P_Environment);
        }
        return environment;
    }
    public void setEnvironment(Environment newValue) {
        Environment old = this.environment;
        fireBeforePropertyChange(P_Environment, old, newValue);
        this.environment = newValue;
        firePropertyChange(P_Environment, old, this.environment);
    }
    @OAProperty(isFkeyOnly = true)
    @OAColumn(name = "EnvironmentId")
    public Integer getEnvironmentId() {
        return (Integer) getFkeyProperty(P_EnvironmentId);
    }
    public void setEnvironmentId(Integer newValue) {
        this.environment = null;
        setFkeyProperty(P_EnvironmentId, newValue);
    }

    @OAMany(
        displayName = "Server Applications", 
        toClass = ServerApplication.class, 
        owner = true, 
        reverseName = ServerApplication.P_Server, 
        cascadeSave = true, 
        cascadeDelete = true
    )
    public Hub<ServerApplication> getServerApplications() {
        if (hubServerApplications == null) {
            hubServerApplications = (Hub<ServerApplication>) getHub(P_ServerApplications);
        }
        return hubServerApplications;
    }
    public void load(ResultSet rs, int id) throws SQLException {
        this.id = id;
        java.sql.Timestamp timestamp;
        timestamp = rs.getTimestamp(2);
        if (timestamp != null) this.created = new OADateTime(timestamp);
        this.name = rs.getString(3);
        this.host = rs.getString(4);
        this.ipAddress = rs.getString(5);
        int appUserFkey = rs.getInt(6);
        setFkeyProperty(P_AppUser, rs.wasNull() ? null : appUserFkey);
        int environmentFkey = rs.getInt(7);
        setFkeyProperty(P_Environment, rs.wasNull() ? null : environmentFkey);

        this.changedFlag = false;
        this.newFlag = false;
    }
}
 
