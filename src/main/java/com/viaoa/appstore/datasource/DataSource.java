// Generated by OABuilder

package com.viaoa.appstore.datasource;

import java.util.logging.Level;
import java.util.logging.Logger;
import java.sql.*;
import com.viaoa.object.*;
import com.viaoa.util.*;
import com.viaoa.object.OAAnnotationDelegate;
import com.viaoa.annotation.OATable;
import com.viaoa.datasource.jdbc.*;
import com.viaoa.datasource.jdbc.db.Database;
import com.viaoa.datasource.jdbc.db.Table;
import com.viaoa.datasource.jdbc.db.Column;
import com.viaoa.datasource.jdbc.db.Index;
import com.viaoa.datasource.jdbc.db.*;
import com.viaoa.appstore.resource.Resource;
import com.viaoa.appstore.model.oa.*;

public class DataSource {
    private static Logger LOG = Logger.getLogger(DataSource.class.getName());
    protected OADataSourceJDBC jdbcDataSource;
    protected Database database;
    protected DBMetaData dbmd;
    
    public DataSource() {
    }
    
    public void open() throws Exception {
        String driver = Resource.getValue(Resource.DB_JDBC_Driver);
        String jdbcUrl = Resource.getValue(Resource.DB_JDBC_URL);
        String user = Resource.getValue(Resource.DB_User);
        String pw = Resource.getValue(Resource.DB_Password);
        String pwBase64 = Resource.getValue(Resource.DB_Password_Base64);
        int dbmdType = Resource.getInt(Resource.DB_DBMD_Type);
        int minConnections = Resource.getInt(Resource.DB_MinConnections);
        int maxConnections = Resource.getInt(Resource.DB_MaxConnections);
    
        if (OAString.isEmpty(pw)) {
            String s = Base64.decode(pwBase64);
            if (!OAString.isEmpty(s)) pw = s;
        }
        open(driver, jdbcUrl, dbmdType, user, pw, minConnections, maxConnections);
    }
    
    protected void open(String driver, String jdbcUrl, int dbmdType, String user, String password, int min, int max) throws Exception {
        if (jdbcDataSource != null) return;
        String s = String.format("JDBC: driver=%s, url=%s, dbmdType=%d, user=%s", driver, jdbcUrl, dbmdType, user);
        LOG.fine(s);
        Database db = getDatabase();
    
        createDAO(db);
        dbmd = new DBMetaData(dbmdType, user, password, driver, jdbcUrl);
        dbmd.setMinConnections(min);
        dbmd.setMaxConnections(max);
        jdbcDataSource = new OADataSourceJDBC(db, dbmd);
    }
    
    public DBMetaData getDBMetaData() {
        return dbmd;
    }
    public void close() {
        getOADataSource().close();
        jdbcDataSource = null;;
    }
    
    
    public OADataSourceJDBC getOADataSource() {
        return jdbcDataSource;
    }

    public Database getDatabase() {
        if (database != null) return database;
        try {
            database = createDatabaseFromClasses();
        }
        catch (Exception e) {
            throw new RuntimeException("error creating database", e);
        }
        return database;
    }    
    
    private Database createDatabaseFromClasses() throws Exception {
        Database database = new Database();
    
        String packageName = ApplicationType.class.getPackage().getName();
        String[] fnames = OAReflect.getClasses(packageName);
    
        Class[] classes = null;
        for (String fn : fnames) {
            Class c = Class.forName(packageName + "." + fn);
            if (c.getAnnotation(OATable.class) == null) continue;
            classes = (Class[]) OAArray.add(Class.class, classes, c);
        }
        OAAnnotationDelegate.update(database, classes);
        return database;
    }
    private Database createDatabase() {
        int NextNumber = 0;
        // TABLES
        int APPLICATIONTYPE = 1;
        int APPLICATIONVERSION = 2;
        int APPRUNTIME = 3;
        int APPRUNTIMEERROR = 4;
        int APPSERVER = 5;
        int APPUSER = 6;
        int APPUSERERROR = 7;
        int APPUSERLOGIN = 8;
        int CLIENTAPP = 9;
        int ENVIRONMENT = 10;
        int RUNNINGAPP = 11;
        int SERVER = 12;
        int SERVERAPPLICATION = 13;
        int SINGLEAPP = 14;
        
        // LINK TABLES
        int APPUSERAPPLICATIONTYPE = 15;
        int MAX = 16;
        
        Database db = new Database();
        Table[] tables = new Table[MAX];
        Column[] columns;
        Link[] links;
        Column[] fkeys;
        
        // TABLES
        tables[NextNumber] = new Table("NextNumber",com.viaoa.datasource.autonumber.NextNumber.class); // ** Used by all OADataSource Database
        tables[APPLICATIONTYPE] = new Table("ApplicationType", ApplicationType.class);
        tables[APPLICATIONVERSION] = new Table("ApplicationVersion", ApplicationVersion.class);
        tables[APPRUNTIME] = new Table("AppRuntime", AppRuntime.class);
        tables[APPRUNTIMEERROR] = new Table("AppRuntimeError", AppRuntimeError.class);
        tables[APPSERVER] = new Table("AppServer", AppServer.class);
        tables[APPUSER] = new Table("AppUser", AppUser.class);
        tables[APPUSERERROR] = new Table("AppUserError", AppUserError.class);
        tables[APPUSERLOGIN] = new Table("AppUserLogin", AppUserLogin.class);
        tables[CLIENTAPP] = new Table("ClientApp", ClientApp.class);
        tables[ENVIRONMENT] = new Table("Environment", Environment.class);
        tables[RUNNINGAPP] = new Table("RunningApp", RunningApp.class);
        tables[SERVER] = new Table("Server", Server.class);
        tables[SERVERAPPLICATION] = new Table("ServerApplication", ServerApplication.class);
        tables[SINGLEAPP] = new Table("SingleApp", SingleApp.class);
        
        // LINK TABLES
        tables[APPUSERAPPLICATIONTYPE] = new Table("AppUserApplicationType",true);
        
        // TABLE COLUMNS
        // NextNumber COLUMNS
        columns = new Column[2];
        columns[0] = new Column("nextNumberId","nextNumberId", Types.VARCHAR, 75);
        columns[0].primaryKey = true;
        columns[1] = new Column("nextNumber","nextNumber", Types.INTEGER);
        tables[NextNumber].setColumns(columns);
        
        // ApplicationType COLUMNS
        columns = new Column[14];
        columns[0] = new Column("Id", "id", Types.INTEGER, 20);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("Created", "created", Types.TIMESTAMP);
        columns[2] = new Column("Name", "name", Types.VARCHAR, 40);
        columns[3] = new Column("AbbrevName", "abbrevName", Types.VARCHAR, 8);
        columns[4] = new Column("DirectoryName", "directoryName", Types.VARCHAR, 45);
        columns[5] = new Column("SingleTypeOnly", "singleTypeOnly", Types.BOOLEAN);
        columns[6] = new Column("ClientPort", "clientPort", Types.INTEGER);
        columns[7] = new Column("HttpPort", "httpPort", Types.INTEGER);
        columns[8] = new Column("HttpsPort", "httpsPort", Types.INTEGER);
        columns[9] = new Column("DownloadUrl", "downloadUrl", Types.VARCHAR, 125);
        columns[10] = new Column("JarFileName", "jarFileName", Types.VARCHAR, 70);
        columns[11] = new Column("MainClass", "mainClass", Types.VARCHAR, 75);
        columns[12] = new Column("JvmOptions", "jvmOptions", Types.VARCHAR, 120);
        columns[13] = new Column("Icon", "icon", Types.BLOB);
        tables[APPLICATIONTYPE].setColumns(columns);
        
        // ApplicationVersion COLUMNS
        columns = new Column[7];
        columns[0] = new Column("Id", "id", Types.INTEGER, 20);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("Created", "created", Types.TIMESTAMP);
        columns[2] = new Column("Version", "version", Types.VARCHAR, 12);
        columns[3] = new Column("Release", "release", Types.INTEGER);
        columns[4] = new Column("Completed", "completed", Types.TIMESTAMP);
        columns[5] = new Column("Verified", "verified", Types.TIMESTAMP);
        columns[6] = new Column("ApplicationTypeId", true);
        tables[APPLICATIONVERSION].setColumns(columns);
        tables[APPLICATIONVERSION].addIndex(new Index("ApplicationVersionApplicationType", "ApplicationTypeId", true));
        
        // AppRuntime COLUMNS
        columns = new Column[7];
        columns[0] = new Column("Id", "id", Types.INTEGER, 20);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("Created", "created", Types.DATE);
        columns[2] = new Column("EnvName", "envName", Types.VARCHAR, 20);
        columns[3] = new Column("IsProd", "isProd", Types.BOOLEAN);
        columns[4] = new Column("Started", "started", Types.TIMESTAMP);
        columns[5] = new Column("Stopped", "stopped", Types.TIMESTAMP);
        columns[6] = new Column("Version", "version", Types.VARCHAR, 20);
        tables[APPRUNTIME].setColumns(columns);
        
        // AppRuntimeError COLUMNS
        columns = new Column[6];
        columns[0] = new Column("Id", "id", Types.INTEGER, 20);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("Created", "created", Types.TIMESTAMP);
        columns[2] = new Column("Message", "message", Types.VARCHAR, 0);
        columns[3] = new Column("Acknowledged", "ack", Types.BOOLEAN);
        columns[4] = new Column("StackTrace", "stackTrace", Types.CLOB);
        columns[5] = new Column("AppRuntimeId", true);
        tables[APPRUNTIMEERROR].setColumns(columns);
        tables[APPRUNTIMEERROR].addIndex(new Index("AppRuntimeErrorAppRuntime", "AppRuntimeId", true));
        
        // AppServer COLUMNS
        columns = new Column[6];
        columns[0] = new Column("Id", "id", Types.INTEGER, 5);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("Created", "created", Types.TIMESTAMP);
        columns[2] = new Column("Started", "started", Types.TIMESTAMP);
        columns[3] = new Column("DemoMode", "demoMode", Types.BOOLEAN);
        columns[4] = new Column("Release", "release", Types.VARCHAR, 18);
        columns[5] = new Column("AppUserLoginId", true);
        tables[APPSERVER].setColumns(columns);
        tables[APPSERVER].addIndex(new Index("AppServerAppUserLogin", "AppUserLoginId", true));
        
        // AppUser COLUMNS
        columns = new Column[10];
        columns[0] = new Column("Id", "id", Types.INTEGER, 5);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("LoginId", "loginId", Types.VARCHAR, 24);
        columns[2] = new Column("Password", "password", Types.VARCHAR, 50);
        columns[3] = new Column("Admin", "admin", Types.BOOLEAN);
        columns[4] = new Column("EditProcessed", "editProcessed", Types.BOOLEAN);
        columns[5] = new Column("FirstName", "firstName", Types.VARCHAR, 30);
        columns[6] = new Column("LastName", "lastName", Types.VARCHAR, 55);
        columns[7] = new Column("InactiveDate", "inactiveDate", Types.DATE);
        columns[8] = new Column("Note", "note", Types.CLOB);
        columns[9] = new Column("DisableAutoLogin", "disableAutoLogin", Types.BOOLEAN);
        tables[APPUSER].setColumns(columns);
        
        // AppUserError COLUMNS
        columns = new Column[7];
        columns[0] = new Column("Id", "id", Types.INTEGER, 5);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("DateTimeValue", "dateTime", Types.TIMESTAMP);
        columns[2] = new Column("Message", "message", Types.VARCHAR, 250);
        columns[3] = new Column("StackTrace", "stackTrace", Types.CLOB);
        columns[4] = new Column("Reviewed", "reviewed", Types.DATE);
        columns[5] = new Column("ReviewNote", "reviewNote", Types.VARCHAR, 254);
        columns[6] = new Column("AppUserLoginId", true);
        tables[APPUSERERROR].setColumns(columns);
        tables[APPUSERERROR].addIndex(new Index("AppUserErrorAppUserLogin", "AppUserLoginId", true));
        
        // AppUserLogin COLUMNS
        columns = new Column[12];
        columns[0] = new Column("Id", "id", Types.INTEGER, 5);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("Created", "created", Types.TIMESTAMP);
        columns[2] = new Column("Location", "location", Types.VARCHAR, 50);
        columns[3] = new Column("ComputerName", "computerName", Types.VARCHAR, 32);
        columns[4] = new Column("TimeZone", "timeZone", Types.VARCHAR, 60);
        columns[5] = new Column("Disconnected", "disconnected", Types.TIMESTAMP);
        columns[6] = new Column("ConnectionId", "connectionId", Types.INTEGER);
        columns[7] = new Column("HostName", "hostName", Types.VARCHAR, 35);
        columns[8] = new Column("IpAddress", "ipAddress", Types.VARCHAR, 20);
        columns[9] = new Column("TotalMemory", "totalMemory", Types.BIGINT);
        columns[10] = new Column("FreeMemory", "freeMemory", Types.BIGINT);
        columns[11] = new Column("AppUserId", true);
        tables[APPUSERLOGIN].setColumns(columns);
        tables[APPUSERLOGIN].addIndex(new Index("AppUserLoginAppUser", "AppUserId", true));
        
        // ClientApp COLUMNS
        columns = new Column[5];
        columns[0] = new Column("Id", "id", Types.INTEGER, 20);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("Created", "created", Types.TIMESTAMP);
        columns[2] = new Column("AppUserId", true);
        columns[3] = new Column("RunningAppId", true);
        columns[4] = new Column("ServerApplicationId", true);
        tables[CLIENTAPP].setColumns(columns);
        tables[CLIENTAPP].addIndex(new Index("ClientAppAppUser", "AppUserId", true));
        tables[CLIENTAPP].addIndex(new Index("ClientAppRunningApp", "RunningAppId", true));
        tables[CLIENTAPP].addIndex(new Index("ClientAppServerApplication", "ServerApplicationId", true));
        
        // Environment COLUMNS
        columns = new Column[6];
        columns[0] = new Column("Id", "id", Types.INTEGER, 20);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("Created", "created", Types.TIMESTAMP);
        columns[2] = new Column("Name", "name", Types.VARCHAR, 35);
        columns[3] = new Column("AbbrevName", "abbrevName", Types.VARCHAR, 8);
        columns[4] = new Column("Type", "type", Types.INTEGER);
        columns[5] = new Column("Seq", "seq", Types.INTEGER);
        tables[ENVIRONMENT].setColumns(columns);
        
        // RunningApp COLUMNS
        columns = new Column[8];
        columns[0] = new Column("Id", "id", Types.INTEGER, 20);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("Created", "created", Types.TIMESTAMP);
        columns[2] = new Column("StopRequest", "stopRequest", Types.TIMESTAMP);
        columns[3] = new Column("Pid", "pid", Types.BIGINT);
        columns[4] = new Column("Error", "error", Types.VARCHAR, 240);
        columns[5] = new Column("CpuSeconds", "cpuSeconds", Types.BIGINT);
        columns[6] = new Column("Stopped", "stopped", Types.TIMESTAMP);
        columns[7] = new Column("AppUserLoginId", true);
        tables[RUNNINGAPP].setColumns(columns);
        tables[RUNNINGAPP].addIndex(new Index("RunningAppAppUserLogin", "AppUserLoginId", true));
        
        // Server COLUMNS
        columns = new Column[7];
        columns[0] = new Column("Id", "id", Types.INTEGER, 20);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("Created", "created", Types.TIMESTAMP);
        columns[2] = new Column("Name", "name", Types.VARCHAR, 55);
        columns[3] = new Column("Host", "host", Types.VARCHAR, 45);
        columns[4] = new Column("IpAddress", "ipAddress", Types.VARCHAR, 20);
        columns[5] = new Column("AppUserId", true);
        columns[6] = new Column("EnvironmentId", true);
        tables[SERVER].setColumns(columns);
        tables[SERVER].addIndex(new Index("ServerAppUser", "AppUserId", true));
        tables[SERVER].addIndex(new Index("ServerEnvironment", "EnvironmentId", true));
        
        // ServerApplication COLUMNS
        columns = new Column[10];
        columns[0] = new Column("Id", "id", Types.INTEGER, 20);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("Created", "created", Types.TIMESTAMP);
        columns[2] = new Column("ClientPort", "clientPort", Types.INTEGER);
        columns[3] = new Column("HttpPort", "httpPort", Types.INTEGER);
        columns[4] = new Column("HttpsPort", "httpsPort", Types.INTEGER);
        columns[5] = new Column("LastConnect", "lastConnect", Types.TIMESTAMP);
        columns[6] = new Column("ApplicationTypeId", true);
        columns[7] = new Column("ApplicationVersionId", true);
        columns[8] = new Column("RunningAppId", true);
        columns[9] = new Column("ServerId", true);
        tables[SERVERAPPLICATION].setColumns(columns);
        tables[SERVERAPPLICATION].addIndex(new Index("ServerApplicationApplicationType", "ApplicationTypeId", true));
        tables[SERVERAPPLICATION].addIndex(new Index("ServerApplicationApplicationVersion", "ApplicationVersionId", true));
        tables[SERVERAPPLICATION].addIndex(new Index("ServerApplicationRunningApp", "RunningAppId", true));
        tables[SERVERAPPLICATION].addIndex(new Index("ServerApplicationServer", "ServerId", true));
        
        // SingleApp COLUMNS
        columns = new Column[6];
        columns[0] = new Column("Id", "id", Types.INTEGER, 20);
        columns[0].primaryKey = true;
        columns[0].assignNextNumber = true;
        columns[1] = new Column("Created", "created", Types.TIMESTAMP);
        columns[2] = new Column("ApplicationTypeId", true);
        columns[3] = new Column("ApplicationVersionId", true);
        columns[4] = new Column("AppUserId", true);
        columns[5] = new Column("RunningAppId", true);
        tables[SINGLEAPP].setColumns(columns);
        tables[SINGLEAPP].addIndex(new Index("SingleAppApplicationType", "ApplicationTypeId", true));
        tables[SINGLEAPP].addIndex(new Index("SingleAppApplicationVersion", "ApplicationVersionId", true));
        tables[SINGLEAPP].addIndex(new Index("SingleAppAppUser", "AppUserId", true));
        tables[SINGLEAPP].addIndex(new Index("SingleAppRunningApp", "RunningAppId", true));
        
        // Link Tables Columns
        
        // AppUserApplicationType COLUMNS
        columns = new Column[2];
        columns[0] = new Column("AppUserId",null);
        columns[1] = new Column("ApplicationTypeId",null);
        tables[APPUSERAPPLICATIONTYPE].setColumns(columns);
        tables[APPUSERAPPLICATIONTYPE].addIndex(new Index("ApplicationTypeAppUser", new String[] {"AppUserId"}));
        tables[APPUSERAPPLICATIONTYPE].addIndex(new Index("AppUserApplicationType", new String[] {"ApplicationTypeId"}));
        
        // LINKS
        // table.addLink( propertyName, toTableName, reversePropertyName, FKey ColumnNumber(s))
        tables[APPLICATIONTYPE].addLink("applicationVersions", tables[APPLICATIONVERSION], "applicationType", new int[] {0});
        tables[APPLICATIONTYPE].addLink("serverApplications", tables[SERVERAPPLICATION], "applicationType", new int[] {0});
        tables[APPLICATIONTYPE].addLink("singleApps", tables[SINGLEAPP], "applicationType", new int[] {0});
        tables[APPLICATIONVERSION].addLink("applicationType", tables[APPLICATIONTYPE], "applicationVersions", new int[] {6});
        tables[APPLICATIONVERSION].addLink("serverApplications", tables[SERVERAPPLICATION], "applicationVersion", new int[] {0});
        tables[APPLICATIONVERSION].addLink("singleApps", tables[SINGLEAPP], "applicationVersion", new int[] {0});
        tables[APPRUNTIME].addLink("appRuntimeErrors", tables[APPRUNTIMEERROR], "appRuntime", new int[] {0});
        tables[APPRUNTIMEERROR].addLink("appRuntime", tables[APPRUNTIME], "appRuntimeErrors", new int[] {5});
        tables[APPSERVER].addLink("appUserLogin", tables[APPUSERLOGIN], "appServers", new int[] {5});
        tables[APPUSER].addLink("appUserLogins", tables[APPUSERLOGIN], "appUser", new int[] {0});
        tables[APPUSER].addLink("clientApps", tables[CLIENTAPP], "appUser", new int[] {0});
        tables[APPUSER].addLink("server", tables[SERVER], "appUser", new int[] {0});
        tables[APPUSER].addLink("singleApps", tables[SINGLEAPP], "appUser", new int[] {0});
        tables[APPUSERERROR].addLink("appUserLogin", tables[APPUSERLOGIN], "appUserErrors", new int[] {6});
        tables[APPUSERLOGIN].addLink("appServers", tables[APPSERVER], "appUserLogin", new int[] {0});
        tables[APPUSERLOGIN].addLink("appUser", tables[APPUSER], "appUserLogins", new int[] {11});
        tables[APPUSERLOGIN].addLink("appUserErrors", tables[APPUSERERROR], "appUserLogin", new int[] {0});
        tables[APPUSERLOGIN].addLink("runningApps", tables[RUNNINGAPP], "appUserLogin", new int[] {0});
        tables[CLIENTAPP].addLink("appUser", tables[APPUSER], "clientApps", new int[] {2});
        tables[CLIENTAPP].addLink("runningApp", tables[RUNNINGAPP], "clientApp", new int[] {3});
        tables[CLIENTAPP].addLink("serverApplication", tables[SERVERAPPLICATION], "clientApps", new int[] {4});
        tables[ENVIRONMENT].addLink("servers", tables[SERVER], "environment", new int[] {0});
        tables[RUNNINGAPP].addLink("appUserLogin", tables[APPUSERLOGIN], "runningApps", new int[] {7});
        tables[RUNNINGAPP].addLink("clientApp", tables[CLIENTAPP], "runningApp", new int[] {0});
        tables[RUNNINGAPP].addLink("serverApplication", tables[SERVERAPPLICATION], "runningApp", new int[] {0});
        tables[RUNNINGAPP].addLink("singleApp", tables[SINGLEAPP], "runningApp", new int[] {0});
        tables[SERVER].addLink("appUser", tables[APPUSER], "server", new int[] {5});
        tables[SERVER].addLink("environment", tables[ENVIRONMENT], "servers", new int[] {6});
        tables[SERVER].addLink("serverApplications", tables[SERVERAPPLICATION], "server", new int[] {0});
        tables[SERVERAPPLICATION].addLink("applicationType", tables[APPLICATIONTYPE], "serverApplications", new int[] {6});
        tables[SERVERAPPLICATION].addLink("applicationVersion", tables[APPLICATIONVERSION], "serverApplications", new int[] {7});
        tables[SERVERAPPLICATION].addLink("clientApps", tables[CLIENTAPP], "serverApplication", new int[] {0});
        tables[SERVERAPPLICATION].addLink("runningApp", tables[RUNNINGAPP], "serverApplication", new int[] {8});
        tables[SERVERAPPLICATION].addLink("server", tables[SERVER], "serverApplications", new int[] {9});
        tables[SINGLEAPP].addLink("applicationType", tables[APPLICATIONTYPE], "singleApps", new int[] {2});
        tables[SINGLEAPP].addLink("applicationVersion", tables[APPLICATIONVERSION], "singleApps", new int[] {3});
        tables[SINGLEAPP].addLink("appUser", tables[APPUSER], "singleApps", new int[] {4});
        tables[SINGLEAPP].addLink("runningApp", tables[RUNNINGAPP], "singleApp", new int[] {5});
        
        // Links for Link Tables
        
        // AppUserApplicationType LINKS
        tables[APPUSERAPPLICATIONTYPE].addLink("appUsers", tables[APPUSER], "applicationTypes", new int[] {0});
        tables[APPUSER].addLink("applicationTypes", tables[APPUSERAPPLICATIONTYPE], "appUsers", new int[] {0});
        tables[APPUSERAPPLICATIONTYPE].addLink("applicationTypes", tables[APPLICATIONTYPE], "appUsers", new int[] {1});
        tables[APPLICATIONTYPE].addLink("appUsers", tables[APPUSERAPPLICATIONTYPE], "applicationTypes", new int[] {0});
        db.setTables(tables);
        return db;
    }
    
    protected void createDAO(Database db) {
        DataAccessObject dao;
        Table table;
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "ApplicationType.Id";
            private static final String columns = "ApplicationType.Id, ApplicationType.Created, ApplicationType.Name, ApplicationType.AbbrevName, ApplicationType.DirectoryName, ApplicationType.SingleTypeOnly, ApplicationType.ClientPort, ApplicationType.HttpPort, ApplicationType.HttpsPort, ApplicationType.DownloadUrl, ApplicationType.JarFileName, ApplicationType.MainClass, ApplicationType.JvmOptions";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getApplicationType(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("ApplicationType");
        if (table != null) table.setDataAccessObject(dao);
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "ApplicationVersion.Id";
            private static final String columns = "ApplicationVersion.Id, ApplicationVersion.Created, ApplicationVersion.Version, ApplicationVersion.Release, ApplicationVersion.Completed, ApplicationVersion.Verified, ApplicationVersion.ApplicationTypeId";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getApplicationVersion(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("ApplicationVersion");
        if (table != null) table.setDataAccessObject(dao);
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "AppRuntime.Id";
            private static final String columns = "AppRuntime.Id, AppRuntime.Created, AppRuntime.EnvName, AppRuntime.IsProd, AppRuntime.Started, AppRuntime.Stopped, AppRuntime.Version";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getAppRuntime(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("AppRuntime");
        if (table != null) table.setDataAccessObject(dao);
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "AppRuntimeError.Id";
            private static final String columns = "AppRuntimeError.Id, AppRuntimeError.Created, AppRuntimeError.Message, AppRuntimeError.Acknowledged, AppRuntimeError.StackTrace, AppRuntimeError.AppRuntimeId";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getAppRuntimeError(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("AppRuntimeError");
        if (table != null) table.setDataAccessObject(dao);
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "AppServer.Id";
            private static final String columns = "AppServer.Id, AppServer.Created, AppServer.Started, AppServer.DemoMode, AppServer.Release, AppServer.AppUserLoginId";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getAppServer(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("AppServer");
        if (table != null) table.setDataAccessObject(dao);
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "AppUser.Id";
            private static final String columns = "AppUser.Id, AppUser.LoginId, AppUser.Password, AppUser.Admin, AppUser.EditProcessed, AppUser.FirstName, AppUser.LastName, AppUser.InactiveDate, AppUser.Note, AppUser.DisableAutoLogin";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getAppUser(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("AppUser");
        if (table != null) table.setDataAccessObject(dao);
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "AppUserError.Id";
            private static final String columns = "AppUserError.Id, AppUserError.DateTimeValue, AppUserError.Message, AppUserError.StackTrace, AppUserError.Reviewed, AppUserError.ReviewNote, AppUserError.AppUserLoginId";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getAppUserError(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("AppUserError");
        if (table != null) table.setDataAccessObject(dao);
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "AppUserLogin.Id";
            private static final String columns = "AppUserLogin.Id, AppUserLogin.Created, AppUserLogin.Location, AppUserLogin.ComputerName, AppUserLogin.TimeZone, AppUserLogin.Disconnected, AppUserLogin.ConnectionId, AppUserLogin.HostName, AppUserLogin.IpAddress, AppUserLogin.TotalMemory, AppUserLogin.FreeMemory, AppUserLogin.AppUserId";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getAppUserLogin(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("AppUserLogin");
        if (table != null) table.setDataAccessObject(dao);
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "ClientApp.Id";
            private static final String columns = "ClientApp.Id, ClientApp.Created, ClientApp.AppUserId, ClientApp.RunningAppId, ClientApp.ServerApplicationId";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getClientApp(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("ClientApp");
        if (table != null) table.setDataAccessObject(dao);
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "Environment.Id";
            private static final String columns = "Environment.Id, Environment.Created, Environment.Name, Environment.AbbrevName, Environment.Type, Environment.Seq";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getEnvironment(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("Environment");
        if (table != null) table.setDataAccessObject(dao);
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "RunningApp.Id";
            private static final String columns = "RunningApp.Id, RunningApp.Created, RunningApp.StopRequest, RunningApp.Pid, RunningApp.Error, RunningApp.CpuSeconds, RunningApp.Stopped, RunningApp.AppUserLoginId";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getRunningApp(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("RunningApp");
        if (table != null) table.setDataAccessObject(dao);
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "Server.Id";
            private static final String columns = "Server.Id, Server.Created, Server.Name, Server.Host, Server.IpAddress, Server.AppUserId, Server.EnvironmentId";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getServer(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("Server");
        if (table != null) table.setDataAccessObject(dao);
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "ServerApplication.Id";
            private static final String columns = "ServerApplication.Id, ServerApplication.Created, ServerApplication.ClientPort, ServerApplication.HttpPort, ServerApplication.HttpsPort, ServerApplication.LastConnect, ServerApplication.ApplicationTypeId, ServerApplication.ApplicationVersionId, ServerApplication.RunningAppId, ServerApplication.ServerId";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getServerApplication(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("ServerApplication");
        if (table != null) table.setDataAccessObject(dao);
        
        dao = new DataAccessObject() {
            private static final String pkeyColumns = "SingleApp.Id";
            private static final String columns = "SingleApp.Id, SingleApp.Created, SingleApp.ApplicationTypeId, SingleApp.ApplicationVersionId, SingleApp.AppUserId, SingleApp.RunningAppId";
            @Override
            public String getPkeySelectColumns() {
                return pkeyColumns;
            }
            @Override
            public String getSelectColumns() {
                return columns;
            }
            @Override
            public OAObject getObject(DataAccessObject.ResultSetInfo rsi) throws SQLException {
                return getSingleApp(rsi.getResultSet(), rsi);
            }
        };
        table = db.getTable("SingleApp");
        if (table != null) table.setDataAccessObject(dao);
    }
    
    protected ApplicationType getApplicationType(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        ApplicationType applicationType = (ApplicationType) OAObjectCacheDelegate.getObject(ApplicationType.class, id);
        if (applicationType == null) {
            applicationType = new ApplicationType();
            applicationType.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return applicationType;
    }
    
    protected ApplicationVersion getApplicationVersion(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        ApplicationVersion applicationVersion = (ApplicationVersion) OAObjectCacheDelegate.getObject(ApplicationVersion.class, id);
        if (applicationVersion == null) {
            applicationVersion = new ApplicationVersion();
            applicationVersion.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return applicationVersion;
    }
    
    protected AppRuntime getAppRuntime(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        AppRuntime appRuntime = (AppRuntime) OAObjectCacheDelegate.getObject(AppRuntime.class, id);
        if (appRuntime == null) {
            appRuntime = new AppRuntime();
            appRuntime.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return appRuntime;
    }
    
    protected AppRuntimeError getAppRuntimeError(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        AppRuntimeError appRuntimeError = (AppRuntimeError) OAObjectCacheDelegate.getObject(AppRuntimeError.class, id);
        if (appRuntimeError == null) {
            appRuntimeError = new AppRuntimeError();
            appRuntimeError.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return appRuntimeError;
    }
    
    protected AppServer getAppServer(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        AppServer appServer = (AppServer) OAObjectCacheDelegate.getObject(AppServer.class, id);
        if (appServer == null) {
            appServer = new AppServer();
            appServer.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return appServer;
    }
    
    protected AppUser getAppUser(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        AppUser appUser = (AppUser) OAObjectCacheDelegate.getObject(AppUser.class, id);
        if (appUser == null) {
            appUser = new AppUser();
            appUser.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return appUser;
    }
    
    protected AppUserError getAppUserError(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        AppUserError appUserError = (AppUserError) OAObjectCacheDelegate.getObject(AppUserError.class, id);
        if (appUserError == null) {
            appUserError = new AppUserError();
            appUserError.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return appUserError;
    }
    
    protected AppUserLogin getAppUserLogin(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        AppUserLogin appUserLogin = (AppUserLogin) OAObjectCacheDelegate.getObject(AppUserLogin.class, id);
        if (appUserLogin == null) {
            appUserLogin = new AppUserLogin();
            appUserLogin.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return appUserLogin;
    }
    
    protected ClientApp getClientApp(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        ClientApp clientApp = (ClientApp) OAObjectCacheDelegate.getObject(ClientApp.class, id);
        if (clientApp == null) {
            clientApp = new ClientApp();
            clientApp.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return clientApp;
    }
    
    protected Environment getEnvironment(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        Environment environment = (Environment) OAObjectCacheDelegate.getObject(Environment.class, id);
        if (environment == null) {
            environment = new Environment();
            environment.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return environment;
    }
    
    protected RunningApp getRunningApp(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        RunningApp runningApp = (RunningApp) OAObjectCacheDelegate.getObject(RunningApp.class, id);
        if (runningApp == null) {
            runningApp = new RunningApp();
            runningApp.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return runningApp;
    }
    
    protected Server getServer(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        Server server = (Server) OAObjectCacheDelegate.getObject(Server.class, id);
        if (server == null) {
            server = new Server();
            server.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return server;
    }
    
    protected ServerApplication getServerApplication(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        ServerApplication serverApplication = (ServerApplication) OAObjectCacheDelegate.getObject(ServerApplication.class, id);
        if (serverApplication == null) {
            serverApplication = new ServerApplication();
            serverApplication.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return serverApplication;
    }
    
    protected SingleApp getSingleApp(ResultSet rs, DataAccessObject.ResultSetInfo rsi) throws SQLException {
        int id = rs.getInt(1);
        SingleApp singleApp = (SingleApp) OAObjectCacheDelegate.getObject(SingleApp.class, id);
        if (singleApp == null) {
            singleApp = new SingleApp();
            singleApp.load(rs, id);
        }
        else {
            rsi.setFoundInCache(true);
        }
        return singleApp;
    }
}
