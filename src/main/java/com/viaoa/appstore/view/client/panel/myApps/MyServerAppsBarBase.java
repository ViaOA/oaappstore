// Generated by OABuilder 
package com.viaoa.appstore.view.client.panel.myApps;

import java.awt.*;
import java.awt.event.*;
import java.util.logging.*;
import javax.swing.*;
import com.viaoa.appstore.delegate.ModelDelegate;
import com.viaoa.appstore.model.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.search.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.appstore.resource.Resource;
import com.viaoa.appstore.view.oa.*;
import com.viaoa.hub.*;
import com.viaoa.jfc.*;

public class MyServerAppsBarBase {
    private static Logger LOG = Logger.getLogger(MyServerAppsBarBase.class.getName());
    
    protected JPanel cardPanel;
    protected CardLayout cardLayout;
    protected OATree tree;
    
    public static final String CARD_Splash = "splash";
    public static final String CARD_AppUserServerServerApplications = "AppUserServerServerApplications";
    
    public MyServerAppsBarBase() {
        cardLayout = new CardLayout();
        cardPanel = new JPanel(cardLayout);
        setup();
    }
    
    public JPanel getPanel() {
        return cardPanel;
    }
    
    protected void setup() {
        ImageIcon icon = Resource.getJarIcon("myAppsSplash.png");
        JLabel lbl = new JLabel("My Server Apps", icon, JLabel.CENTER);
        lbl.setFont(lbl.getFont().deriveFont(20.0f));
        lbl.setVerticalTextPosition(SwingConstants.TOP);
        lbl.setHorizontalTextPosition(SwingConstants.CENTER);
        cardPanel.add(lbl, CARD_Splash);
        cardLayout.show(cardPanel, CARD_Splash);
    
        getTree();
        OATreeTitleNode ttNode;
    
        // AppUserServerServerApplications - My Server Applications - 
        Hub<ServerApplication> hubServerApplication = new Hub<ServerApplication>(ServerApplication.class);
        new HubMerger(ModelDelegate.getLocalAppUserHub(), hubServerApplication, AppUserPP.server().serverApplications().pp, false);
        ServerApplicationJfc jfcServerApplication = new ServerApplicationJfc(hubServerApplication) {
            JPanel panx;
            @Override
            public void showCardPanel(final String name) {
                MyServerAppsBarBase.this.cardLayout.show(MyServerAppsBarBase.this.cardPanel, CARD_AppUserServerServerApplications);
                if (panx != null) {
                    super.showCardPanel(name);
                    return;
                }
                SwingWorker<Void, Void> sw = new SwingWorker<Void, Void>() {
                    volatile Exception ex;
                    @Override
                    protected Void doInBackground() throws Exception {
                        try {
                            panx = getCardPanel();
                        }
                        catch (Exception e) {
                            ex = e;
                        }
                        return null;
                    }
                    @Override
                    protected void done() {
                        if (ex != null) {
                            LOG.log(Level.WARNING, "UI exception creating ServerApplicationJfc", ex);
                            JOptionPane.showMessageDialog(null, "Exception while creating UI, message sent to tech support", "UI Exception", JOptionPane.ERROR_MESSAGE);
                        }
                        if (panx == null) panx = new JPanel();
                        MyServerAppsBarBase.this.cardPanel.add(panx, CARD_AppUserServerServerApplications);
                        showCardPanel(name);
                    }
                };
                sw.execute();
            }
        };
        jfcServerApplication.getModel().setPluralDisplayName("My Server Applications");
        jfcServerApplication.getModel().setAllowAdd(false);
        jfcServerApplication.getModel().setAllowDownload(true);
        jfcServerApplication.getModel().setAllowRemove(false);
        jfcServerApplication.getModel().setAllowSearch(false);
        jfcServerApplication.getModel().setAllowHubSearch(true);
        // user profile
        jfcServerApplication.getModel().setAllowDelete(false);
        jfcServerApplication.getModel().setAllowClear(false);
        jfcServerApplication.getModel().setAllowCut(false);
        jfcServerApplication.getModel().setAllowPaste(false);
        jfcServerApplication.getModel().setAllowNew(false);
        ttNode = jfcServerApplication.getTreeTitleNode();
        tree.add(ttNode);
        ttNode.add(jfcServerApplication.getTreeNode());
        cardPanel.add(new JLabel("loading My Server Applications ...", Resource.getJarIcon("wait.png"), JLabel.CENTER), CARD_AppUserServerServerApplications);
    }
    protected OATree getTree() {
        if (tree != null) return tree;
        tree = new OATree(18) {
            @Override
            public void nodeSelected(OATreeNode node) {
                super.nodeSelected(node);
                if (node == null) {
                    MyServerAppsBarBase.this.cardLayout.show(MyServerAppsBarBase.this.cardPanel, MyServerAppsBarBase.this.CARD_Splash);
                }
            }
        };
        tree.setAllowDnD(true);
        return tree;
    }
}

