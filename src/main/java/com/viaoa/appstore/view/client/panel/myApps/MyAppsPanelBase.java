// Generated by OABuilder 
package com.viaoa.appstore.view.client.panel.myApps;

import java.awt.*;
import java.awt.event.*;
import java.util.logging.Logger;
import javax.swing.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.appstore.delegate.*;
import com.viaoa.appstore.model.search.*;
import com.viaoa.appstore.resource.Resource;
import com.viaoa.appstore.view.oa.*;
import com.viaoa.hub.*;
import com.viaoa.jfc.*;
import com.viaoa.jfc.control.*;
import com.viaoa.jfc.border.ShadowBorder;

public class MyAppsPanelBase {
    private static Logger LOG = Logger.getLogger(MyAppsPanelBase.class.getName());
    
    private JRadioButtonMenuItem mi; 
    private JToggleButton toggleButton;
    
    private JSplitPane splitPane;
    private OAOutlookBar outlookBar;
    protected JPanel cardPanel;
    protected CardLayout cardLayout;
    
    public static final String CARD_MySingleUserAppsBar = "MySingleUserAppsBar";
    public static final String CARD_MyServerAppsBar = "MyServerAppsBar";
    public static final String CARD_MyClientApssBar = "MyClientApssBar";
    
    protected MySingleUserAppsBar barMySingleUserApps;
    protected MyServerAppsBar barMyServerApps;
    protected MyClientApssBar barMyClientApss;
    
    public MyAppsPanelBase() {
        cardLayout = new CardLayout();
        cardPanel = new JPanel(cardLayout);
        
        splitPane = new OASplitPane(JSplitPane.HORIZONTAL_SPLIT, getOutlookBar(), cardPanel);
        splitPane.setOneTouchExpandable(true);
        splitPane.setDividerSize(12);
        splitPane.setResizeWeight(.05d);
        
        ImageIcon icon;
        icon = Resource.getJarIcon("myAppsBar.png");
        outlookBar.addBar(CARD_MySingleUserAppsBar, "My Single User Apps", icon, new JScrollPane(getMySingleUserAppsBar().getTree()));
        cardPanel.add(getMySingleUserAppsBar().getPanel(), CARD_MySingleUserAppsBar);
        icon = Resource.getJarIcon("myAppsBar.png");
        outlookBar.addBar(CARD_MyServerAppsBar, "My Server Apps", icon, new JScrollPane(getMyServerAppsBar().getTree()));
        cardPanel.add(getMyServerAppsBar().getPanel(), CARD_MyServerAppsBar);
        icon = Resource.getJarIcon("myAppsBar.png");
        outlookBar.addBar(CARD_MyClientApssBar, "My Client Apss", icon, new JScrollPane(getMyClientApssBar().getTree()));
        cardPanel.add(getMyClientApssBar().getPanel(), CARD_MyClientApssBar);
    }
    
    public MySingleUserAppsBar getMySingleUserAppsBar() {
        if (barMySingleUserApps != null) return barMySingleUserApps;
        barMySingleUserApps = new MySingleUserAppsBar();
        return barMySingleUserApps;
    }
    public MyServerAppsBar getMyServerAppsBar() {
        if (barMyServerApps != null) return barMyServerApps;
        barMyServerApps = new MyServerAppsBar();
        return barMyServerApps;
    }
    public MyClientApssBar getMyClientApssBar() {
        if (barMyClientApss != null) return barMyClientApss;
        barMyClientApss = new MyClientApssBar();
        return barMyClientApss;
    }
    
    public OAOutlookBar getOutlookBar() {
        if (outlookBar != null) return outlookBar;
        outlookBar = new OAOutlookBar() {
            @Override
            protected void onBarSelected(String barName) {
                cardLayout.show(cardPanel, barName);
            }
        };
        int w = OAJfcUtil.getCharWidth(23);
        outlookBar.setPreferredSize(new Dimension(w, 50));
        outlookBar.setBorder(new ShadowBorder());
        Dimension d = outlookBar.getMinimumSize();
        w = OAJfcUtil.getCharWidth(1);
        d.width = w;
        outlookBar.setMinimumSize(d);
        outlookBar.setMinimumSize(new Dimension(10,10));
        return outlookBar;
    }
    
    public JToggleButton getToggleButton() {
        if (toggleButton != null) return toggleButton;
        toggleButton = new JToggleButton("My Apps");
        ImageIcon ii = Resource.getJarIcon("myAppsButton.png");
        toggleButton.setIcon(ii);
        toggleButton.setVerticalTextPosition(SwingConstants.BOTTOM);
        toggleButton.setHorizontalTextPosition(SwingConstants.CENTER);
        toggleButton.setMnemonic('M');
        toggleButton.setToolTipText("show the My Apps panel");
        return toggleButton;
    }
    
    public JRadioButtonMenuItem getMenuItem() {
        if (mi != null) return mi;
        mi = new JRadioButtonMenuItem();
        mi.setMnemonic(KeyEvent.VK_M);
        mi.setAccelerator(KeyStroke.getKeyStroke(KeyEvent.VK_M, InputEvent.ALT_MASK, false));
        mi.setText("My Apps panel");
        mi.setToolTipText("show the MyApps panel");
        return mi;
    }
    
    public JSplitPane getSplitPane() {
        return splitPane;
    }
    public JComponent getMainComponent() {
        return splitPane;
    }
}

