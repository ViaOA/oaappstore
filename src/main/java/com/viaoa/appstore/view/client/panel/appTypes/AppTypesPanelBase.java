// Generated by OABuilder 
package com.viaoa.appstore.view.client.panel.appTypes;

import java.awt.*;
import java.awt.event.*;
import java.util.logging.Logger;
import javax.swing.*;
import com.viaoa.appstore.model.oa.*;
import com.viaoa.appstore.model.oa.propertypath.*;
import com.viaoa.appstore.delegate.*;
import com.viaoa.appstore.model.search.*;
import com.viaoa.appstore.resource.Resource;
import com.viaoa.appstore.view.oa.*;
import com.viaoa.hub.*;
import com.viaoa.jfc.*;
import com.viaoa.jfc.control.*;
import com.viaoa.jfc.border.ShadowBorder;

public class AppTypesPanelBase {
    private static Logger LOG = Logger.getLogger(AppTypesPanelBase.class.getName());
    
    private JRadioButtonMenuItem mi; 
    private JToggleButton toggleButton;
    
    private JSplitPane splitPane;
    private OAOutlookBar outlookBar;
    protected JPanel cardPanel;
    protected CardLayout cardLayout;
    
    public static final String CARD_ApplicationTypesBar = "ApplicationTypesBar";
    public static final String CARD_MyAppTypesBar = "MyAppTypesBar";
    
    protected ApplicationTypesBar barApplicationTypes;
    protected MyAppTypesBar barMyAppTypes;
    
    public AppTypesPanelBase() {
        cardLayout = new CardLayout();
        cardPanel = new JPanel(cardLayout);
        
        splitPane = new OASplitPane(JSplitPane.HORIZONTAL_SPLIT, getOutlookBar(), cardPanel);
        splitPane.setOneTouchExpandable(true);
        splitPane.setDividerSize(12);
        splitPane.setResizeWeight(.05d);
        
        ImageIcon icon;
        icon = Resource.getJarIcon("appTypesBar.png");
        outlookBar.addBar(CARD_ApplicationTypesBar, "Application Types", icon, new JScrollPane(getApplicationTypesBar().getTree()));
        cardPanel.add(getApplicationTypesBar().getPanel(), CARD_ApplicationTypesBar);
        icon = Resource.getJarIcon("appTypesBar.png");
        outlookBar.addBar(CARD_MyAppTypesBar, "My App Types", icon, new JScrollPane(getMyAppTypesBar().getTree()));
        cardPanel.add(getMyAppTypesBar().getPanel(), CARD_MyAppTypesBar);
    }
    
    public ApplicationTypesBar getApplicationTypesBar() {
        if (barApplicationTypes != null) return barApplicationTypes;
        barApplicationTypes = new ApplicationTypesBar();
        return barApplicationTypes;
    }
    public MyAppTypesBar getMyAppTypesBar() {
        if (barMyAppTypes != null) return barMyAppTypes;
        barMyAppTypes = new MyAppTypesBar();
        return barMyAppTypes;
    }
    
    public OAOutlookBar getOutlookBar() {
        if (outlookBar != null) return outlookBar;
        outlookBar = new OAOutlookBar() {
            @Override
            protected void onBarSelected(String barName) {
                cardLayout.show(cardPanel, barName);
            }
        };
        int w = OAJfcUtil.getCharWidth(23);
        outlookBar.setPreferredSize(new Dimension(w, 50));
        outlookBar.setBorder(new ShadowBorder());
        Dimension d = outlookBar.getMinimumSize();
        w = OAJfcUtil.getCharWidth(1);
        d.width = w;
        outlookBar.setMinimumSize(d);
        outlookBar.setMinimumSize(new Dimension(10,10));
        return outlookBar;
    }
    
    public JToggleButton getToggleButton() {
        if (toggleButton != null) return toggleButton;
        toggleButton = new JToggleButton("App Types");
        ImageIcon ii = Resource.getJarIcon("appTypesButton.png");
        toggleButton.setIcon(ii);
        toggleButton.setVerticalTextPosition(SwingConstants.BOTTOM);
        toggleButton.setHorizontalTextPosition(SwingConstants.CENTER);
        toggleButton.setMnemonic('A');
        toggleButton.setToolTipText("show the App Types panel");
        return toggleButton;
    }
    
    public JRadioButtonMenuItem getMenuItem() {
        if (mi != null) return mi;
        mi = new JRadioButtonMenuItem();
        mi.setMnemonic(KeyEvent.VK_A);
        mi.setAccelerator(KeyStroke.getKeyStroke(KeyEvent.VK_A, InputEvent.ALT_MASK, false));
        mi.setText("App Types panel");
        mi.setToolTipText("show the AppTypes panel");
        return mi;
    }
    
    public JSplitPane getSplitPane() {
        return splitPane;
    }
    public JComponent getMainComponent() {
        return splitPane;
    }
}

